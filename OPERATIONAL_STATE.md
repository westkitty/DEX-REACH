# DEX//REACH Operational State

<!-- operational-state:metadata
{"schema_version":1,"project_id":"dex-reach","project_name":"DEX//REACH","project_root":".","artifact_path":"","state_revision":61,"last_updated":"2026-09-23T16:51:05Z","current_baseline":{"identity":"Release closure complete: installed runtime code 31bf8a1 passed real Claude Code payload plus six-stage trace acceptance; final PR head a682f627 passed validate, runtime-proof, reproducible-build and CodeQL; PR #2 merged to main as 6bf7880b30ad8b709324b537cf52297698598b66.","state":"released-merged","last_verified":"2026-09-20T20:22:12Z"},"scope_boundaries":["DEX//REACH gateway, node agent, MCP interface, local service install, Dock control terminal, project docs"],"linked_parent_state":null}
-->

## 1. Project Identity and Scope

DEX//REACH is a secure AI-native remote-computing control plane. It provides useful AI access while preserving device-owner authority, explicit machine selection, bounded operations, revocability, auditability, and fail-closed behavior.

Canonical public repository: `https://github.com/westkitty/DEX-REACH`.

## 2. Current Baseline

The primary macOS deployment is running DEX//REACH **0.3.2** from installed runtime code commit `31bf8a11beebd2aaa52aeac5a495897d3eccf30d`. Installation used the self-host-safe one-shot LaunchAgent path; the node re-registered at **2026-09-20T20:13:29Z**. Direct post-install evidence shows coordinator PID **75441**, workspace worker PID **75446**, gateway PID **75460**, and node PID **75507**, each `state = running`, `runs = 1`, and `last exit code = (never exited)`. Loopback `/healthz` returned `{"ok":true,"service":"DEX//REACH","version":"0.3.2","onlineNodes":1}`. `npm run dex -- status` reported the existing `full-local` profile, the existing owner roots, AI access enabled, no capability grants, and the expected persistent node.

A real configured Claude Code 2.1.267 session was then isolated to the single explicit DEX MCP server and allowed only `mcp__dex-reach__reach_fingerprint`. Claude called `reach_fingerprint` exactly once for `node_id = macbook-air.local` and `cwd = /Users/andrew/DEX-REACH`; the tool result contained the useful fingerprint payload in `content` and trace id `7e58bf5e2dff5b8e96bdcd4ec84f0ee0` only in `_meta['dex-reach/trace-id']`. Claude rendered the fingerprint payload successfully. `npm run dex -- trace 7e58bf5e2dff5b8e96bdcd4ec84f0ee0` resolved exactly six causal stages for that same real call: `mcp → gateway → node → authorize → execute → receipt`. This closes the deployed real-client result-contract and final trace-acceptance blocker.

The merged release preserves the owner's local regression-only follow-up for client-preferred structured output in `tests/mcp-contract.test.ts` plus the matching proof-harness explanation in `scripts/lib/live-reach.ts`. Those files do not change the coordinator/worker/gateway/node runtime installed at `31bf8a1`. The final PR head `a682f6274b32113362dee3232d79f2368f4e3d5a` passed GitHub `validate`, `runtime-proof`, `reproducible-build`, and CodeQL, then PR #2 merged into `main` as `6bf7880b30ad8b709324b537cf52297698598b66`.

**2026-09-23 OAuth repair — INSTALLED AND REAL-CLIENT PATH RESTORED; LONG-LIVED REFRESH EXPIRY CYCLE STILL PENDING.** Live primary-Mac evidence showed a healthy 0.3.2 gateway/node and successful OpenAI MCP initialization/tool calls followed by repeated `POST /token 500` responses from the ChatGPT OAuth connector. PR #3 repaired the source by advertising opt-in `offline_access` while still requiring `mcp:tools` for resource authority, returning typed OAuth grant/scope/token errors, and allowing the same refresh credential to survive repeated or concurrent refresh attempts until normal expiry or revocation. Regression coverage proves that `offline_access` alone cannot authorize MCP access, two concurrent refreshes can use one refresh credential safely, and invalid refresh credentials surface as `invalid_grant`. Tested PR head `70f4d3dee1c28e88be4498fd115fc7451de4e2dd` passed GitHub `validate`, `runtime-proof`, `reproducible-build`, and CodeQL, then PR #3 merged as `2343ffa60f09382f9329d450a226ad1e24161d23`. The owner then reported pulling through `badbc03`, running `npm ci` with 0 vulnerabilities, installing all four launchd services, seeing the node and gateway healthy with AI access enabled, and loopback `/healthz` returning one online node. A subsequent real ChatGPT DEX call reached the gateway but was rejected by Origin validation before tool execution, so long-lived refresh behavior remains unproven end-to-end.

**2026-09-23 public Origin repair — INSTALLED AND VERIFIED THROUGH REAL CHATGPT.** The first post-install ChatGPT `reach_list_nodes` attempt reached the public gateway but returned `Invalid Origin`. Repository inspection showed the gateway supplied the configured public hostname to the MCP SDK as `allowedHosts` only; because the service binds to loopback and `allowedOrigins` was omitted, the SDK independently armed localhost-only Origin validation. PR #4 derives one exact hostname allowlist from the configured public base URL plus loopback and passes it to both Host and Origin validation, preserving DNS-rebinding/CSRF protection while admitting the configured public MCP origin. Its integration regression proves configured public Host+Origin succeeds while unrelated Host or Origin values remain 403. Tested head `cc6d3ac6c3db708798dfef2c3626a0549fcd8b2d` passed typecheck, invariant check, full tests, build, production dependency audit, runtime proof, reproducible clean build, and CodeQL; PR #4 merged as `fd1ffad695ec5bff7175c420c5f13c4d2f2b5372`. The owner installed the merge on the primary Mac. A fresh real ChatGPT `reach_list_nodes` call then succeeded and returned `macbook-air.local` online with `full-local` profile, expected roots `/Users/andrew`, `/tmp`, `/private/tmp`, AI mode `on`, 22 compatibility tools, four DEX services, healthy scheduler pressure, and no queued/active work. A fresh `reach_fingerprint` from ChatGPT for `/Users/andrew/DEX-REACH` returned hostname `MacBook-Air.local`, user `andrew`, platform `darwin/arm64`, repository root `/Users/andrew/DEX-REACH`, branch `main`, remote `git@github.com:westkitty/DEX-REACH.git`, Node `v26.7.0`, and Python `3.14.7`. `reach_repo_info` reported a clean `main...origin/main` worktree at `ad8882e` immediately before this closure update. This closes the Origin incident and proves the current ChatGPT -> public gateway -> OAuth -> MCP -> node read-only path end to end.

**2026-09-23 OAuth reliability hardening — INSTALLED AND LIVE-PROVEN; FINAL MERGED-MAIN GOLDEN RERUN PENDING.** PR #5 replaced the frozen `@modelcontextprotocol/server-legacy` authorization-server router with DEX-owned OAuth routes, preserved Dynamic Client Registration compatibility, added tightly scoped ChatGPT CIMD support, enforced S256 PKCE plus redirect/resource binding, added deterministic short-TTL access-expiry -> repeated/concurrent refresh regression coverage, made expected invalid refresh credentials return OAuth `invalid_grant` 4xx rather than opaque 500, added share-safe OAuth/token diagnostics, made token-endpoint 5xx a `verify:golden` failure, and added a persisted authenticated public-path canary scheduled at load and every six hours. PR #5 merged as `7f63003264abadb8389ff33155cdeff32728bd44`. The owner then pulled through `e1ab0f028b5545a8423319a9b06d062e6742d4e0`, ran `npm ci` with 0 vulnerabilities, and `npm run install:macos`; the one-shot helper completed all coordinator/worker/gateway/node/canary replacements successfully. The node re-registered and this ChatGPT session again completed live `reach_list_nodes`, `reach_fingerprint`, and `reach_repo_info` against `macbook-air.local`. The scheduled canary was manually kickstarted after the node returned; launchd recorded run count 2 with last exit code 0, and `doctor --share` recorded canary `ok=true`, node online, refresh credential present. A live public smoke then passed the full 16-action OAuth/MCP path and `oauth:health` reported token2xx=2, token4xx=0, token5xx=0. PR #6 (`14effb8e43c1833edc761785d65e334a588f3d6b`) removed the remaining SEP-2352 discovery-binding warning from the golden smoke provider; its head passed the full hosted validation/runtime-proof/reproducible-build/CodeQL matrix. A subsequent local full golden run exposed a Mac-only coordinator test cold-start timeout while Linux CI passed. PR #7 increases only that test wait window from ~2s to ~10s; head `566b804469c6f8aff3ecfc96ef36a63d69fad5ba` passed full validation, runtime proof, reproducible build, and CodeQL, then merged as `993ef1dc4dc8c453fa444a7dc0e3b8479c5860e9`. The production OAuth hardening itself is installed and proven; the only remaining closure item is to pull `993ef1d` on the primary Mac and run one final local `verify:golden` after the currently offline node is recovered.

Current public server surface:

- **16 first-class DEX MCP actions**, including the evidence-scoped `reach_trust_report`;
- **22 remotely exposed compatibility tools**;
- the pinned compatibility dependency still has **26 raw local tools internally**, but safety-configuration mutation, local compatibility call history, vendor feedback, and vendor onboarding/prompt tools are withheld from remote clients;
- compatibility URL-fetch mode is blocked;
- native child processes and the isolated compatibility backend do not inherit DEX credential variables or obvious secret-bearing environment variables.

`npm run verify:golden` passes on the deployed 0.3.2 build: typecheck, the synchronized **21-invariant machine manifest**, **51/51 regression tests**, production build, production dependency audit with **0 vulnerabilities**, raw 26-tool compatibility probe, and live public smoke covering OAuth, **16 DEX tools**, the evidence-scoped trust report, the exact 22-tool safe compatibility surface, blocked configuration/URL-proxy paths, ADB executable availability, native file/process execution, sanitized child environment, refusal of a deliberately wrong expected identity, refusal after real Git branch drift between plan and commit, successful identity-bound plan→commit, signed receipts, and checkpoint creation.

The coordinator hardening, owner-visible activity ledger, resource-aware work bundles, queue telemetry, node-to-gateway scheduler summaries, and coordinator LaunchAgent are now **installed on the primary Mac**. `npm run dex -- work-status --share` from the installed tree reports one substantive/heavy slot on this 8 GiB Mac, queue-depth and oldest/p50/p95 latency fields, and zero active leases/queued work at the observed idle point. `npm run dex -- activity --share` reports the installed activity view without local paths or PIDs.

> **RELEASE CANDIDATE — ACCEPTED AND MERGED.** Installed runtime code `31bf8a1` restarted all four persistent services cleanly and returned the useful MCP payload to both ChatGPT and a real configured Claude Code client. Claude's accepted call carried trace id `7e58bf5e2dff5b8e96bdcd4ec84f0ee0` in result `_meta`, and the same id resolved the complete six-stage causal chain. At the post-acceptance idle check the coordinator reported **0 active leases**, **0 active heavy work**, and **0 queued tickets**. Final PR head `a682f627` passed validate, runtime-proof, reproducible-build and CodeQL, and PR #2 merged to `main` as `6bf7880b`.

The primary macOS installer is self-host safe: it stages and validates service definitions, returns the invoking DEX request, then a separate no-`KeepAlive` one-shot LaunchAgent replaces gateway/node. The expected brief disconnect is followed by node re-registration; repeated restart loops are not accepted as success.

The Dock launcher is installed as a signed local shell-app bundle with a custom icon and exact Dock entry. A click opens a **new Terminal instance** running the DEX control console. The last launcher-specific proof was on 0.3.1 and showed the node PID unchanged across launcher invocation and the same access mode, profile, and per-client ceilings before and after launch. Version 0.3.2 did not modify the launcher path, so that proof remains historical rather than being relabeled as a fresh launcher test.

This record describes the verified 0.3.2 source/runtime baseline but intentionally does not encode the hash or publication status of the Git commit that contains it. Git itself is authoritative for the containing commit, push state, and hosted checks; a commit cannot truthfully record the result of CI that only runs after that same commit is pushed.

## 3. Artifact Contract

Provide explicit-node remote access for filesystem, search/edit, process/terminal, Git/development, ADB discovery, multi-node routing, MCP/OAuth, local policy, authentication/revocation, capability grants, exact-action planning, signed receipts, bounded results, reversible checkpoints, persistent local operation, owner-visible recovery, and public-source documentation without granting public runtime authority.

## 4. Active Invariants

The detailed proof obligations live in [`docs/INVARIANTS.md`](docs/INVARIANTS.md), and `src/shared/invariants.ts` is the synchronized machine-consumable ID index. There are **42 release-blocking invariants**. The active groups are:

- **DEX-INV-001** — explicit node selection and no routing fallback.
- **DEX-INV-002–003** — node-local owner authority and fail-closed policy.
- **DEX-INV-004–009** — canonical filesystem scope, compatibility ownership, shell-free READ-ONLY, honest ON/full-local limits, child-secret boundaries, and protected transport.
- **DEX-INV-010–013** — exact one-use plans, signed linear receipts, concurrent state safety, and independent revocable credentials.
- **DEX-INV-014** — public source grants no runtime authority.
- **DEX-INV-015–016** — self-update survival and launcher non-escalation.
- **DEX-INV-017** — the public MCP surface is exactly the intended 16-action contract.
- **DEX-INV-018** — ADB availability is reported honestly.
- **DEX-INV-019** — simulation is never upgraded into physical-hardware proof.
- **DEX-INV-020** — planned mutations bind a fresh execution fingerprint and commit refuses identity drift.
- **DEX-INV-021** — live trust-report PASS is explicitly limited to the checks it actually evaluates.
- **DEX-INV-022–026** — shared-machine work coordination is now installed on the primary Mac behind the persistent coordinator daemon. Admission grants no execution authority, repository mutation ownership is exclusive, exhausted capacity queues rather than oversubscribing, stale claims expire without terminating processes, and coordination metadata carries no prompts, transcripts, or credentials. Regression coverage plus installed `work-status --share`, queue telemetry, and daemon-backed cross-path smoke support the current claim.
- **DEX-INV-027** — causal tracing links stages without exporting content (regression-only).
- **DEX-INV-028** — workspace-safe narrows execution and is itself narrowed by owner authority. Regression-covered and source-live proven on the primary Mac with a real temporary gateway/node/OAuth/MCP pair configured `workspace-safe`; the persistent installed 0.3.2 node remains `full-local`.
- **DEX-INV-029** — rolling execution budgets only narrow authority. Regression-covered and source-live proven on the primary Mac with a real temporary node: two live MCP requests passed under a two-operation ceiling, the third was refused by the rolling budget, and the owner-policy hash stayed unchanged. The installed 0.3.2 node still has no budget configured.
- **DEX-INV-030** — capability requests never grant authority; only local owner approval creates an ordinary grant (regression-only; not a public MCP action).
- **DEX-INV-031** — custom policy assertions run before owner-policy persist; policy history is append-only and restore creates a new revision (regression-only).
- **DEX-INV-032** — node transport authentication is a separate cryptographic domain from receipt signing (regression-only; no live asymmetric enrollment on the deployed gateway).
- **DEX-INV-033–039** — replay resistance, adapter authority declaration, experimental node-local secret boundaries, portable evidence, reproducible release provenance, honest proof reconciliation, and OAuth discovery/response agreement. Their proof states remain as recorded in `docs/INVARIANTS.md`.
- **DEX-INV-040** — owner-visible activity identifies DEX-owned processes without persisting command content; activity evidence grants no authority. Regression-covered and now installed-runtime exercised on the primary Mac via `dex activity --share`; share mode exposed scheduler/activity state without local paths or PIDs.
- **DEX-INV-041** — machine coordination and activity have one namespace per OS account. Installed-runtime cross-path proof on the primary Mac acquired a lease through the normal owner CLI, then a compatibility shell with isolated `HOME=/Users/andrew/.dex-reach/compat-home` observed the same active lease through the coordinator daemon. Authority-bearing state keeps its separate `stateDir()` semantics.
- **DEX-INV-042** — the coordinator daemon transport is owner-private and fail-closed: restrictive bind permissions apply from first exposure, foreign-owned or non-socket paths are refused, frames are bounded/validated, and the client does not silently fall back beside an ambiguous scheduler. Regression/CI proof is green on the current branch.

## 5. Verified Working Behavior

- **VER-001 — Persistent primary Mac services:** gateway/node run under launchd and the node re-registers after service replacement.
- **VER-002 — Public OAuth/MCP path:** dynamic registration, owner approval, PKCE, token exchange, initialization, tool listing, routing, and live execution pass through the deployed HTTPS gateway.
- **VER-003 — 16-tool DEX surface:** the deployed 0.3.2 smoke sees all 16 first-class actions with expected metadata, including `reach_trust_report`.
- **VER-004 — Safe compatibility surface:** raw pinned backend probe reports 26 tools; the deployed node exposes exactly 22 remotely. `set_config_value` is not remotely callable, and compatibility URL reads are refused.
- **VER-005 — Compatibility safety configuration:** isolated backend telemetry is disabled and configured allowed roots are verified at startup.
- **VER-006 — Native scope/security:** file/process paths, symlink escapes, plural/nested/camelCase compatibility paths, private-state targeting, destructive command classes, and READ-ONLY shell composition are regression-covered.
- **VER-007 — Child environment sanitization:** native and compatibility child environments exclude DEX credential variables/obvious secret-bearing variables; public smoke verifies no credential-bearing variable appears in native `env` output.
- **VER-008 — Policy concurrency:** stale owner-policy writers cannot overwrite newer local decisions; final reservations honor latest policy and atomically consume use-bounded grants.
- **VER-009 — Exact plans:** expiration, policy/client/request binding, single-claim concurrency, argument scrubbing, and deployed plan→commit execution pass.
- **VER-010 — Signed receipts:** signature/tamper/content-omission/chain tests pass; deployed receipts show committed work. A reproduced stale-lock TOCTOU that could fork concurrent chains was repaired; post-fix proof includes five focused lock/receipt stress runs, three complete regression runs, and the final golden worker.
- **VER-011 — Shared state locking:** stale-lock recovery uses ownership/inode identity plus a serialized recovery guard; a 40-contender stale-lock fixture never enters the critical section concurrently.
- **VER-012 — Node authentication/revocation:** concurrent credential writers preserve independent nodes; revocation tombstone mutations are locked/atomic; prior live rotation/revocation proof left unrelated nodes working.
- **VER-013 — Bootstrap concurrency:** two concurrent first-boot processes produce one coherent credential set; exactly one creates state and the other preserves it.
- **VER-014 — OAuth hardening:** approval HTML escapes untrusted dynamic client names; dynamic registration always assigns client identity server-side; OAuth persistence is serialized/atomic.
- **VER-015 — Transport configuration:** non-loopback HTTP public identity and non-loopback cleartext node WebSocket configuration are rejected by tests.
- **VER-016 — Android hardware through DEX:** wireless ADB was established to a real Samsung `SM-X910`; raw `adb devices -l` reported state `device`, `reach_adb_devices` saw the same transport through `macbook-air.local`, and harmless identity reads through DEX returned manufacturer `samsung`, model `SM-X910`, Android `16`, SDK `36`. No APK install, reboot, file push/delete, rooting, account mutation, or device-setting change was performed.
- **VER-017 — Self-hosted macOS install:** `npm run install:macos` invoked through DEX returns before service replacement, node reconnects on 0.3.1, and the helper does not enter a restart loop.
- **VER-018 — Dock control terminal:** final bundle signature verifies, exact Dock tile is present, clicking opens a new Terminal instance showing DEX status/kill-switch/timed-mode/audit/receipt/grant controls, and launcher invocation preserves node PID and owner authority state.
- **VER-019 — Local quality gate:** `npm run verify` passes with the 21-invariant manifest check, 53/53 tests, build success, production audit 0 vulnerabilities, and raw compatibility probe.
- **VER-020 — Deployed golden worker:** `npm run verify:golden` passes on the installed 0.3.2 runtime with the complete smoke evidence listed in the baseline above.
- **VER-021 — Prior hosted workflows:** GitHub validation and CodeQL completed successfully for prior commit `0084650`; this proves the workflows themselves execute, not yet that the current unpushed 0.3.1 candidate is green remotely.
- **VER-022 — Multi-node policy simulation:** the isolated second-node simulation previously verified explicit routing/no-fallback, OFF/READ-ONLY/ON, roots, client ceilings, audit attribution, rotation/revocation, and primary-node isolation through the public MCP path.
- **VER-028 — Current ChatGPT end-to-end recovery proof:** after PR #3 OAuth refresh hardening and PR #4 public-Origin repair were installed, this ChatGPT session successfully executed `reach_list_nodes` against the deployed public MCP endpoint, saw `macbook-air.local` online, then obtained a fresh execution fingerprint and Git repository inspection for `/Users/andrew/DEX-REACH`. This proves the live ChatGPT -> OAuth -> public gateway -> MCP routing -> primary Mac node path for current read-only actions. The client still exposes the historical 12-action cached schema, so newer plan/commit/receipt/trust actions remain a separate client-cache issue.
- **VER-023 — Real-client historical proof:** ChatGPT and Claude previously completed OAuth/PKCE and live DEX calls/policy refusals against the deployed project. Those historical client proofs remain valid for the tested behavior; the current 16-action server surface still needs client-side action-cache refresh proof if this already-open ChatGPT connector session is expected to expose the four newer plan/commit/receipt/trust actions immediately.

**VER-024 — Execution-identity binding:** `reach_plan` optionally rejects caller-supplied expected-identity mismatches, every successful new plan stores a fresh fingerprint/hash, and `reach_commit_plan` rechecks that identity before mutation. Public smoke proved both wrong-identity refusal and a real `main` → `identity-drift` branch change refusal without creating the planned file.
- **VER-025 — Live trust certificate:** public smoke successfully called `reach_trust_report`; it returned PASS with a certificate hash and the synchronized invariant inventory while explicitly limiting PASS to its listed live checks.
- **VER-026 — Local project portfolio controls:** the built owner CLI discovers Git repositories beneath the bounded home root, reports branch, dirty count, and upstream divergence, filters dirty repositories, resolves one project uniquely, and can checkpoint that exact project through the existing DEX checkpoint primitive. Real-Mac smoke enumerated 103 repositories under `/Users/andrew`; focused portfolio tests pass 2/2 and the complete local verification gate passes 53/53.
- **VER-027 — Source shared-machine contention on the primary Mac:** a native source `chatgpt` mutate lease held `/Users/andrew/DEX-REACH` while a compatibility-shell source process labelled `claude-code` requested the same medium mutate lease. It queued rather than oversubscribing, and native `work-status`, `work-queue`, and `dex activity` all saw that same queued contender. This run also exposed and then proved the repair for a split coordinator namespace caused by compatibility `HOME` virtualization.
- **VER-028 — Source workspace-safe node on the primary Mac:** a real temporary source gateway, node agent, OAuth/MCP client, and Git worktree ran as separate processes on this Mac with the node configured `workspace-safe`. Through the real MCP path, `reach_file_write` and `reach_checkpoint` succeeded, `reach_process_run`, compatibility `start_process`, and session/process inspection were refused, and switching the owner mode to READ-ONLY preserved fingerprint inspection while refusing writes/checkpoints without re-admitting shell. The probe exited 0 and tore the temporary pair down; the installed 0.3.2 node was not changed.
- **VER-029 — Source rolling-budget enforcement on the primary Mac:** a real temporary source gateway/node/OAuth/MCP pair configured an `other`-client rolling budget of two operations per hour. The first two live `reach_fingerprint` calls passed, the third was refused specifically because the rolling budget was exhausted, and the owner AccessState hash was identical before budget configuration and after usage. Budget policy/usage therefore narrowed execution without mutating owner authority state; the installed 0.3.2 node was not changed.
- **VER-030 — Installed coordinator daemon and scheduler telemetry:** commit `7a0e9c1` is both local HEAD and remote branch head; launchd runs coordinator PID 92898, gateway PID 92903, and node PID 92908 from the repository, each with one run and no exit. Installed `work-status --share` exposes slot ceilings, pressure, queue depth, and oldest/p50/p95 wait metrics; `activity --share` exposes owner-safe activity state.
- **VER-031 — Installed one-machine coordination namespace:** after installation, a lease acquired by the normal owner CLI was visible from a compatibility shell running under the isolated compatibility HOME, proving the persistent daemon prevents native/compatibility execution paths from splitting the scheduler namespace.
- **VER-032 — Reconciled GitHub hardening installed on the primary Mac:** the local docs reconciliation was rebased on top of GitHub `6e01e6c`, the resulting head passed the focused 42-test/typecheck/invariant/build/audit/probe gate, and `npm run install:macos` staged all three services. The one-shot reloader produced the expected brief disconnect, after which coordinator/gateway/node re-registered with new PIDs, healthy loopback status, one online node, and unchanged owner policy/roots/profile.
- **VER-033 — Real Claude client action against the installed runtime:** Claude Code was authenticated in the owner HOME, the configured `dex-reach` MCP server was connected, and a non-interactive run pre-authorized only `mcp__dex-reach__reach_fingerprint`; the real DEX tool call succeeded against explicit node `macbook-air.local`. This closes the real-client-action question independently of the later trace-output repair.
- **VER-034 — Current GitHub release candidate:** branch head `1ad4a65b596f5deee2065ab53b828c1fe7b6578d` is mergeable and GitHub reports CodeQL PASS plus DEX validation PASS; the DEX workflow jobs `validate`, `runtime-proof`, and `reproducible-build` all completed successfully. Runtime proof includes the six-stage `mcp → gateway → node → authorize → execute → receipt` loopback chain and the caller-visible trace id in MCP result metadata.
- **VER-035 — Final deployed real-client result and trace acceptance:** installed runtime code `31bf8a11beebd2aaa52aeac5a495897d3eccf30d` re-registered the primary Mac node after the one-shot service replacement. ChatGPT immediately received the full `reach_fingerprint` payload instead of trace-only output. A settings-isolated real Claude Code 2.1.267 session connected only to the configured `dex-reach` MCP server, called only `mcp__dex-reach__reach_fingerprint` for explicit node `macbook-air.local`, received and rendered the complete fingerprint payload, and exposed trace id `7e58bf5e2dff5b8e96bdcd4ec84f0ee0` in `_meta`. `npm run dex -- trace` resolved that same id through `mcp → gateway → node → authorize → execute → receipt`. Post-acceptance launchd evidence shows coordinator, worker, gateway, and node each running with one run/no prior exit; `/healthz` is healthy with one online node; coordinator active leases and queue depth are zero.

## 6. Known Not Working

- **KNOWN-002 — Primary Mac node currently offline after local verification work:** the PR #5 hardening was installed and live-proven, but after later local golden/focused coordinator test work the gateway again reported no online nodes. No fallback node was selected. The exact cause of this latest disconnect has not yet been established; recover the existing launchd node, then rerun the final merged-main golden gate.
- **KNOWN-001 — RESOLVED:** the ChatGPT public MCP Origin rejection was repaired in PR #4, installed on the primary Mac, and closed by successful real ChatGPT `reach_list_nodes`, `reach_fingerprint`, and `reach_repo_info` calls.

No other unresolved confirmed primary-Mac/runtime bug remains in the inspected 0.3.2 scope after the current repair/resweep cycle.

Directly issuing `launchctl kickstart -k` **from the DEX request being killed** is intentionally not a supported self-update mechanism: destroying a transport can destroy its own response. The supported path is `npm run install:macos`, which delegates replacement to the one-shot helper. This is a lifecycle constraint, not an invitation to retry the self-killing path.

## 7. Implemented but Unverified

- **UNV-020 — Final merged-main golden rerun after Mac test portability repair:** PR #5 OAuth hardening is installed and live-proven; scheduled canary, share-safe diagnostics, real ChatGPT calls, live public smoke, and token health have all passed. PR #6/PR #7 only changed test/proof harness code. One final `npm run verify:golden` on primary-Mac `main` at or after merge `993ef1d` remains pending because the node is currently offline.
- **UNV-001 — Long-lived ChatGPT OAuth refresh expiry cycle:** the PR #3 repair is installed and the current reauthorized ChatGPT session successfully reaches the node, but a naturally expired access-token -> refresh-token -> subsequent tool-call cycle has not yet been observed over time.
- **UNV-004 — RESOLVED:** the public-Origin repair is installed and real ChatGPT node enumeration/fingerprinting/repo inspection succeeded.
- **UNV-002 — Second physical device:** no independent second machine has completed the full enrollment/policy matrix. Multi-node evidence is simulation on the primary Mac.
- **UNV-003 — Linux service runtime:** systemd user-unit generation is implemented/shape-tested but has not run on a real Linux host.
- **UNV-004 — macOS node-only fresh-machine install:** the node-only installer uses the same staged one-shot replacement design but has not been exercised on a fresh second Mac.
- **UNV-007 — macOS capacity probes:** live `work-status` on the primary Mac returned parseable memory `warning`, CPU `saturated`, and thermal `unknown`. Fixture tests still cover the parsers; thermal remains unknown on this host.
- **UNV-011 — Rolling execution budgets in the installed runtime:** source enforcement is now live-proven on the primary Mac through a real temporary gateway/node/OAuth/MCP pair. A two-operation budget admitted exactly two `reach_fingerprint` calls and refused the third specifically for budget exhaustion while the AccessState hash remained unchanged before configuration and after usage. The persistent installed 0.3.2 node still has no budget configured, so deployed enforcement remains unverified.
- **UNV-019 — Fresh clean-host install proof:** the current branch has now been installed on the owner's primary Mac and the three-service persistent runtime is live. The proof item `fresh-node-install` remains unverified because this was an upgrade/reinstall on an existing configured machine rather than a pristine macOS host. Do not convert this installation into a clean-machine claim.
- **UNV-018 — The live pair is one host:** the gateway, the node agent and the MCP client in a proof run are separate OS processes on one Linux machine over loopback. That proves routing, transport authentication, owner refusal, revocation and recovery as software. It does not prove that two physically separate machines stay distinct on the wire, and it exercises no network, TLS ingress or Funnel path. The Android and second-machine items remain `UNVERIFIED — HARDWARE NOT AVAILABLE`.
- **UNV-017 — Signed or attested release artifacts:** the release manifest, artifact checksums, the normalized SBOM hash, the platform claims and the clean-build comparison pass 9 focused regression tests, and the comparison was executed for real: 89 artifacts rebuilt byte for byte from `git archive` of `4067898` under a different absolute path with a fresh `npm ci`. Nothing produced here is signed or attested, no release has been published, and the project licence is unchanged at `UNLICENSED` because choosing one is the owner's decision.
- **UNV-016 — Evidence bundles from a deployed node:** the bundle format, the exporter's refusals, offline verification and every distinguished claim pass 12 focused regression tests, including a search of a real exported bundle for content that must never be in it, a fully re-signed forgery that passes every structural claim, and a verification run after the producing state directory was deleted. The owner CLI was exercised end to end in this container. No bundle has been exported from a deployed node, and no third party has verified one on another machine, so portability is proven against a deleted state directory rather than against a genuinely foreign host.
- **UNV-015 — Node-local secret broker (EXPERIMENTAL):** storage, alias metadata, keyed fingerprints, forbidden environment names, the corrupt-store refusal, the independent `secret.use` capability, the refusals on operations and profiles that cannot inject, and the scrubber pass 11 focused regression tests, including a disk-wide search for a real value across every artifact a run writes and an import-graph assertion that only the node executor reaches the resolve path. The owner CLI was exercised for real in this container. No deployed node has brokered a secret, no MCP action exposes the broker, and the scrubber is best effort against a command that transforms or forwards a value rather than echoing it.
- **UNV-014 — Capability adapter contract on a running node:** the manifest decoder, the catalog cross-check, the registry, the per-adapter scoping and the remote-surface derivation pass 18 focused regression tests, and the declared path arguments were probed from the installed adapter's real MCP schemas. No node has started with the adapter registry wired in, so manifest admission at node start, and the refusal to start when an admitted tool is not offered, are typechecked but not runtime-proven.
- **UNV-012 — Capability request remote path:** local request/approve/deny CLI and the node operation `dex.capability.request` pass regression tests, including that a failed approval creates no grant (r27). The public MCP surface remains 16 actions; remote ChatGPT/Claude cannot file a request through MCP until that contract is intentionally expanded.
- **UNV-013 — Asymmetric node authentication on a deployed gateway:** enrollment tokens, proof verification, dual-mode migration, replay refusal including at nonce-cache capacity, per-node scoping of that capacity (see r26 and r27), and private-key isolation pass regression tests. As of r33 a live gateway/node pair has completed the whole ceremony over a real websocket on a Linux loopback pair, including refusal of private-key material at `/node/enroll` and refusal of the bearer token afterwards. No **deployed** gateway has accepted a DexNodeEd25519 websocket, replay and nonce capacity were not exercised against a live gateway, and Keychain storage is not claimed: the private key is a 0600 file.
- **UNV-010 — workspace-safe in the installed runtime:** a real temporary source node on the primary Mac has now run with `DEX_REACH_PROFILE=workspace-safe` and passed typed-write, checkpoint, shell/process refusal, and READ-ONLY composition checks through OAuth/MCP. The persistent installed 0.3.2 node remains `full-local`; this proves source behavior on the Mac, not deployment of the profile.
- **UNV-006 — Current ChatGPT client action refresh:** the public MCP server proves the full 16-action contract, but this current ChatGPT connector session still exposes exactly **12** DEX actions (`reach_list_nodes`, `reach_list_tools`, `reach_call`, `reach_fingerprint`, `reach_repo_info`, `reach_adb_devices`, `reach_checkpoint`, `reach_file_read`, `reach_file_write`, `reach_process_run`, `reach_result_read`, `reach_revoke_node`). The four newer plan/commit/receipt/trust actions remain absent from this session's connector schema; client-cache refresh/relink is still separate from server correctness.

## 8. Unknown or Evidence-Stale State

The current ChatGPT connector action cache remains evidence-stale as described in UNV-006. External client caches and untested hardware/platform paths remain explicitly separated above.

- **OBS-001 — Intermittent connector unavailability under load:** reproduced again during the strengthened heavy proof. The ChatGPT connector returned `UNAVAILABLE` while the proof process continued and later completed successfully. The installed node remained discoverable before/after; gateway PID 947 and node PID 951 have each run continuously for one launchd start, with `runs = 1` and `last exit code = (never exited)`. The safe node audit contains successful ChatGPT calls immediately bracketing the gap but no failed node request corresponding to the dropped connector call, which means that request never reached node execution. `/usr/bin/log show` for both service PIDs over the reproduction window returned no unified-log service event. The private launchd stdout/stderr files live under the protected DEX state directory and remain correctly unreadable through remote execution; that boundary was not bypassed. Evidence therefore separates the incident from a gateway/node process crash or node-local refusal and places the failure upstream of node execution, but does **not** identify whether the responsible component is ChatGPT connector transport, MCP ingress before node routing, or another platform/network layer.

## 9. Pending Work

- **PND-001:** refresh/relink ChatGPT client action discovery if direct UI access to plan/commit/receipts/trust report is required. As of r42 this session still exposes exactly 12 DEX actions, so the cache has not refreshed itself.
- **PND-009:** implement the already-decided authorization split after PND-008: DEX remains the MCP resource server/verifier while token issuance moves off the frozen `@modelcontextprotocol/server-legacy/auth` bridge to a dedicated standards-focused authorization-server/IdP component. Upstream v2 guidance treats the legacy AS helpers as migration code. Specific component/library selection and implementation are deliberately deferred so OAuth redesign does not contaminate the current migration proof.
- **PND-005:** continue replacing compatibility primitives only when equivalent native behavior has equal or stronger proof. Current post-gate candidates are `get_file_info`, `list_directory`, `read_multiple_files`, `create_directory`, and `move_file`; none are added in this release because widening the deliberately frozen 16-action MCP contract would mix feature expansion into the migration proof.

## 10. Active Decisions, Defaults, and Prohibitions

- Repository source is public. Runtime access remains authenticated, explicitly enrolled, node-scoped, locally governed, and fail-closed.
- Nodes connect outbound; only the authenticated gateway may be intentionally public. No raw node listener/shell is exposed.
- Desktop Commander is a pinned replaceable local compatibility adapter with isolated HOME/config state; its hosted relay/app is not required by DEX.
- Authorization-server direction is settled: `server-legacy/auth` is a temporary migration bridge only. Before long-term production on the v2 path, token issuance moves to a dedicated standards-focused authorization-server/IdP component while DEX remains the OAuth resource server and local node authority. Do not introduce a mandatory SaaS/cloud identity dependency merely to satisfy this split.
- Capability requests remain owner-CLI/node-local for the current release. Filing a request grants no authority, but publishing it would expand the exact 16-action MCP contract; revisit that as a separately proved post-gate contract change.
- ON/full-local arbitrary shell is not advertised as OS-level filesystem isolation.
- No force push, destructive Git reset/clean, plaintext credential logging, invented credentials, secret publication, silent path/root/profile/trust widening, or simulation-as-hardware claims.
- `docs/INVARIANTS.md`, `docs/GOLDEN_WORKER.md`, and `docs/INCIDENT_PREVENTION.md` are durable release/repair references; README remains the human-facing overview.

## 11. Validation and Evidence Matrix

| Capability | State | Decisive current evidence |
| --- | --- | --- |
| Source/type/regression/build | reconciled-head focused gate verified; complete local verify exit still unconfirmed | On the primary Mac after rebasing onto `6e01e6c`, **42/42** focused coordinator/machine-capacity/work-run/MCP-contract tests passed, including the foreign-socket ownership regression; typecheck PASS; synchronized **41-invariant** manifest PASS; build PASS; production audit **0 vulnerabilities**; raw backend probe **26 tools**; `git diff --check` PASS. GitHub DEX validation and CodeQL are green on `6e01e6c`. The earlier complete local `npm run verify` final process exit is still not independently captured, so do not relabel that specific local run as green. |
| Shared-machine work coordination | verified installed | Persistent coordinator LaunchAgent is running; installed `work-status --share` reports slot ceilings, pressure, queue depth and latency telemetry. Cross-path smoke proved a compatibility shell with isolated HOME observes the same daemon-owned lease as the owner CLI. |
| Owner activity/process visibility | verified installed | Installed `dex activity --share` reports DEX-owned activities and coordinator summary without local paths/PIDs. The persistent coordinator/gateway/node services are live on the current installed branch. |
| macOS capacity probes | mixed | live `work-status` on the primary Mac returned memory warning, CPU saturated, thermal unknown; parsers also fixture-tested |
| Rolling execution budgets | source-live verified; installed runtime unconfigured | regression coverage plus a real primary-Mac temporary source node: `other` client budget capped at 2 operations/hour, first two live MCP fingerprint calls passed, third was refused by budget exhaustion, and the owner-policy hash was identical before budget configuration and after usage. Installed 0.3.2 node remains unconfigured. |
| Capability requests | regression-verified; deliberately non-public | request is not a grant, owner approval alone creates an ordinary grant, owner may narrow and cannot widen, deny/expiry create none, OFF still wins, and retry cannot mint a second grant. Decision for this release: keep node-local/owner-CLI and preserve the exact 16-action MCP contract. |
| MCP SDK v2 migration | deployed real-client acceptance verified | Current source gate is 42 invariants with the latest proof reporting **20 proven / 0 failed / 2 hardware-unavailable**. Installed runtime code `31bf8a1` passed a real configured Claude Code `reach_fingerprint` call over the deployed MCP path with useful payload visible and trace metadata preserved; the same trace resolved all six causal stages. Fresh-host, Linux and second-physical-machine limits remain separate unverified scope. |
| Centralized operation/risk catalog | regression-only | 18 focused tests including capability-mapping and READ-ONLY admission parity against the pre-catalog implementation, 16-action MCP order, and 26/22/4 compatibility counts; 100 tests total with 99 passing in the same Linux container. No authorization outcome changed by construction and by test. |
| End-to-end causal tracing | verified deployed | Real Claude Code trace `7e58bf5e2dff5b8e96bdcd4ec84f0ee0` from installed runtime `31bf8a1` resolves exactly `mcp → gateway → node → authorize → execute → receipt`; arguments, contents and process output remain absent from trace summaries. |
| workspace-safe execution profile | source-live verified; installed profile unchanged | primary Mac focused live pair: real `workspace-safe` node admitted typed write + checkpoint, refused shell + compatibility process/session tools, and READ-ONLY further narrowed it while preserving fingerprint inspection. Probe exited 0; installed 0.3.2 node remains `full-local`. |
| Phases 6-10 adversarial correction pass | regression-only | 7 focused correction tests plus the full 167-test suite (166 pass; the one failure is the environmental `native.test.ts` case recorded in r15), 33-invariant manifest, build, 0 production vulnerabilities, 26-tool backend probe. Each of the five defects was first reproduced against `22286d6` in a throwaway detached checkout, so the corrections are backed by observed pre-fix behavior rather than by reading. Validation ran in an ephemeral Linux container, not on the primary Mac. |
| Raw compatibility dependency | verified | probe lists 26 pinned local backend tools |
| Remote compatibility surface | verified | live node reports 22; public smoke enforces exact expected set |
| Public MCP/OAuth | verified | live `npm run smoke`: OAuth/PKCE + 15 actions |
| Owner policy/fail closed | verified | policy/grant/concurrency tests + deployed owner CLI |
| Native/compat path guard | verified | symlink/plural/nested/camelCase/private-state regression tests + live refusals |
| Process secret environment | verified | native regression + deployed public `env` smoke |
| Exact plan/commit + execution identity | verified | concurrency/expiry/scrub tests + wrong-identity refusal + real branch-drift refusal + deployed identity-bound plan→commit smoke |
| Signed receipt chain | verified | tamper/chain/concurrency tests + post-TOCTOU stress + deployed receipt smoke |
| Persistent macOS self-update | verified | self-hosted 0.3.2 install returned, expected brief disconnect occurred, new gateway/node PIDs appeared, and the 0.3.2/22-tool node re-registered with unchanged owner authority |
| Dock Terminal launcher | verified | signed bundle, exact Dock URL, new Terminal window, live DEX menu, unchanged PID/policy/profile |
| Live trust certificate | verified | public smoke reports trustReport:true with 16-tool deployed surface, certificate hash, runtime checks, and evidence-scope limitation |
| ADB executable | verified | live `available:true`; no device attached |
| Containing Git commit CI/CodeQL | external-by-construction | inspect GitHub checks for the commit containing this state record; do not infer hosted CI from this file alone |
| Second physical device | unverified | simulated second-node proof only |
| Linux runtime | unverified | generated/shape-tested only |
| Android hardware operation | verified | Samsung SM-X910 connected over wireless ADB; raw ADB state `device`; DEX `reach_adb_devices` saw the same transport; harmless DEX identity reads returned samsung / SM-X910 / Android 16 / SDK 36 |

## 12. Current Change Scope and Impact Radius

The branch migrates the MCP dependency from the monolithic `@modelcontextprotocol/sdk` 1.30 to the v2 package split (`server`, `client`, `node`, `express`, `server-legacy`) at 2.0.0, and zod from 3 to 4. The sessionful `/mcp` route is preserved by `NodeStreamableHTTPServerTransport` from `@modelcontextprotocol/node`, whose `handleRequest(req, res, body)` shape matches the v1 transport it replaces, and the gateway's own OAuth authorization server is preserved by the official `server-legacy/auth` migration bridge. No tool name, description, annotation, schema or ordering changed, and no DEX authority path was touched. Served protocol semantics are deliberately unchanged in this phase; dual-era serving is separate work.

The branch also adds `src/shared/profiles.ts` and the `workspace-safe` execution profile. Owner modes are unchanged at OFF/READ-ONLY/ON; this is a node-side execution profile, configured locally through `DEX_REACH_PROFILE`, that narrows what ON can reach on that node. It admits inspection, file reads, typed writes, checkpoints, planning and the declared-safe compatibility tools, and refuses arbitrary `dex.process.run`, the process and session compatibility tools, privileged and destructive surfaces, safety configuration mutation, and any adapter tool or operation the catalog does not classify. The refusal is evaluated against the node's own configured profile rather than the effective profile an authorization decision produced, because READ-ONLY replaces that effective profile and reading the constraint from it would let READ-ONLY re-admit exactly what the profile refuses; both narrowings apply and neither may widen the other. A planned commit inherits its target's admission, and a compatibility target defers to the exact tool. `dex explain` now reports the policy decision and the profile constraint separately along with whether the operation would actually run. No installed node's profile changed and the default remains `development`.

The branch also adds `src/shared/trace.ts` and end-to-end causal tracing. A request carries a W3C Trace Context from the MCP edge through the gateway and node into authorization, planning, commit and execution, and the node returns the `traceId` to the caller so a completed action can be reconstructed afterwards. Spans are an allowlist of identifiers, stage, outcome and hashes: arguments, file content, process output, raw plan arguments and credentials are structurally refused, a refusal is traced as an outcome class with its message deliberately omitted, and `baggage` is never accepted at all. Inbound `traceparent` is continued only when well formed and `tracestate` only when it validates within W3C bounds; malformed context starts a fresh trace rather than being repaired. Trace storage is local, bounded and outside Git, and OpenTelemetry export is off unless the owner sets `DEX_REACH_OTEL_EXPORT=1`. Two owner CLI commands, `trace` and `traces`, read it back. This phase adds no MCP action, no capability, no dependency and no authorization path; tracing observes decisions and never makes one.

The branch also adds `src/shared/operations.ts`, one catalog holding DEX operation authority metadata: capability, risk class, mutation, plan eligibility, READ-ONLY admission, workspace-safe eligibility and checkpoint strategy for every routed operation, plus a risk classification for all 26 compatibility adapter tools. Capability mapping, READ-ONLY admission, plan targets, checkpoint strategy and the remote compatibility block list now derive from it instead of being restated in `capabilities.ts`, `access.ts`, `node/main.ts`, `gateway/mcp.ts` and `compatibility.ts`. This phase intentionally changes no authorization outcome; parity with the pre-catalog implementation is asserted by test across every operation including unknown ones. `dex.commitPlan` declares that its risk inherits from its planned target rather than carrying a fixed class, and `node/native.ts` gained a second catalog-driven READ-ONLY refusal behind the existing one.

The branch also adds a local capability-request lifecycle in `src/shared/capability-requests.ts`. AI may request a capability, roots, duration and optional use cap; that record is not authority. Only local owner approval creates an ordinary `CapabilityGrant` (the request id is reused as the grant id so retries cannot mint a second grant). Owner approval may narrow and cannot widen. Deny and expiry create no grant. Raw target arguments are not stored. `dex.capability.request` is a classified node operation but is not a public MCP action, so the 16-action contract is unchanged. Added DEX-INV-030.

The branch also adds rolling execution budgets in `src/shared/budget-policy.ts` and `src/shared/budget-usage.ts`. Runtime usage counters are stored separately from AccessState so they cannot change the owner policy hash or invalidate unrelated plans. Budgets only narrow: OFF, READ-ONLY, workspace-safe, client ceilings and grants still win, shared and per-client ceilings intersect by minimum, a preauthorization denial consumes nothing, and a successful reservation keeps its rolling cost after later execution failure while releasing only the inflight concurrency slot. Missing or corrupt budget policy is unrestricted because owner policy remains the authority source; corrupt usage with a real policy fails closed. Canonical lock order is access lock then budget lock; the budget modules never acquire the access lock. Owner CLI: `budgets`, `budget set`, `budget clear`. Added DEX-INV-029. No MCP action and no dependency change.

The `claude/work-coordinator-cpcug3` branch adds a local shared-machine work coordinator: `src/shared/machine-capacity.ts` (host measurement and deterministic slot/pressure policy), `src/shared/work-coordinator.ts` (leases, FIFO queue, bounded history), seven `work-*` owner CLI commands, and invariants DEX-INV-022 through 026. Coordination state lives under `~/.dex-reach/coordinator/` at mode 0700/0600 and is excluded from Git by the existing `.dex-reach/` ignore rule. The change adds no MCP action, no capability, no dependency, and no authorization path: `authorizeOperation` is untouched and a regression test asserts that holding a lease leaves its outcome identical. Machine admission and execution authorization stay separate decisions that must both pass.


The branch now also adds an owner-visible process activity ledger in `src/shared/activity.ts`. Native `dex.process.run` records the actual child PID at spawn, and compatibility `start_process` records the adapter-reported PID; finished, failed, timed-out, terminated and externally exited states remain bounded as owner evidence. The persistent record stores only activity id, kind, state, PID, operation, a bounded executable label, timestamps and local cwd metadata — never raw command arguments, prompts, transcripts, stdout/stderr or credentials — and corrupt activity evidence fails visibly instead of silently disappearing. `dex activity` merges those process records with coordinator leases/tickets, live gateway/node service identities and anonymous heavy blockers; `--share` removes local PIDs and paths. Coordinator leases now record whether a PID was explicitly bound to the workload, so an ordinary short-lived `work-acquire` process is no longer presented as the workload. The Dock control console gains a local Live DEX activity entry. This adds no MCP action, capability, dependency or authorization path; activity is evidence only. Added DEX-INV-040.

The current 0.3.2 source baseline now also adds bounded local portfolio intelligence to the owner CLI: home-root Git discovery, branch/dirty/upstream summaries, dirty-only filtering, unique project resolution, and exact-project checkpointing through the existing checkpoint primitive. This does not widen the public MCP action surface or remote execution authority. It preserves the existing gateway/node authority model, compatibility surface, macOS service lifecycle, launcher path, and private deployment state outside Git.

## 13. Compact Revision Log

- **r56** — Closed publication after r55's installed-client acceptance. Final PR head `a682f6274b32113362dee3232d79f2368f4e3d5a` completed GitHub `validate`, `runtime-proof`, `reproducible-build`, and CodeQL with success. PR #2 was marked ready and merged normally into `main`; GitHub reports merge commit `6bf7880b30ad8b709324b537cf52297698598b66`. A post-merge live scheduler check on the primary Mac showed **0 queued tickets**, **0 active leases**, **0 active heavy work**, and **0 uncoordinated heavy work**. The installed service runtime remains the real-client-accepted `31bf8a1` code; the commits between it and the merge are regression/proof/state/publication changes and do not alter coordinator/worker/gateway/node runtime code. This revision records publication state only.
- **r55** — Closed the release-blocking real-client result/trace gate on the primary Mac. Runtime code `31bf8a1` was installed from a detached exact-SHA checkout while preserving the owner's local `8904461` regression commit on `preserve/pre-final-89044619b383`. The one-shot installer recycled coordinator/worker/gateway/node and the node re-registered at 20:13:29Z. Direct launchd inspection then showed coordinator PID 75441, worker PID 75446, gateway PID 75460 and node PID 75507, each running with `runs = 1` and no prior exit; `/healthz` reported 0.3.2 healthy with one online node and the coordinator showed zero active leases/queue at the acceptance boundary. ChatGPT's live `reach_fingerprint` returned its useful payload immediately. A real settings-isolated Claude Code 2.1.267 session connected only to the configured `dex-reach` MCP server, called only `reach_fingerprint` for `macbook-air.local`, received and rendered the complete payload, and surfaced trace id `7e58bf5e2dff5b8e96bdcd4ec84f0ee0` in result `_meta`; `dex trace` resolved that same call through all six stages: `mcp → gateway → node → authorize → execute → receipt`. The local-only regression patch was then reconstructed on the GitHub branch as equivalent test/proof-harness content; it changes no installed service runtime. PND-010, UNV-008 and UNV-009 are therefore closed. Final exact-head CI and PR #2 merge remain the only release-publication steps and are intentionally not claimed by this commit before GitHub runs them.
- **r54** — Repaired the client-visible result contract the owner's `7c9dadb` blocks the merge on. His note records that a real Claude Code `reach_fingerprint` call against the installed `951818c` reached all six trace stages and returned the trace metadata **without** the fingerprint payload. The cause is `structuredContent`: `959a799` set it to `{ dex_trace_id }`, and structured output is the tool's machine-readable result, so the gateway was declaring that the trace id **is** the result. The payload was on the wire throughout in `content[0].text`; any client that reads structured output in preference to text saw an identifier and nothing else. The caller-visible trace id now travels in `_meta` alone, which is out-of-band metadata and does not stand in for the result — the same reason it was moved off a second content block in r50. **Nothing in this repository could see it**: the source contract test asserted the text payload and then read the trace id *through* `structuredContent`, so it confirmed the field that was hiding the payload, and the live proof parses `trust.text`, so it read `content` too. Both now assert that a routed result reports no structured output and that its payload is non-empty, and `scripts/lib/live-reach.ts` reports the result's structured output so a proof item can assert on the difference rather than on whichever field the harness happens to parse. Counterfactual confirmed twice: reinstating `structuredContent` turns `tests/mcp-contract.test.ts` red and takes the live proof to **18 proven, 1 failed** with `live-mcp-surface` naming the structured output. This is the second time on this branch that a defect visible only to a real client passed every source test, and the second time the harness read one field while a real client read another. Gate in this container: typecheck PASS; invariant manifest PASS at **42**; `npm test` **254 tests, 253 pass**, the single failure the known environmental `native.test.ts` `nvm` case that fails identically at base `80fcbe1`; build PASS; production audit **0 vulnerabilities**; backend probe **26** compatibility tools; `npm run proof -- --require-live` **19 proven, 0 failed, 3 unverified**; `git diff --check` clean. Public MCP action contract unchanged at 16 actions; no invariant, policy, credential, profile or roots change, and no test or gate weakened, skipped or disabled. **This is not re-acceptance.** The repair has not been installed or exercised by a real client on the primary Mac; that remains the owner's, and the merge stays blocked until he does it.
- **r53** — Reviewed the owner's `959a799`/`951818c` (credential-free workspace worker and coordinator progress stream) and repaired three defects. **The one that matters is a DEX-INV-026 violation.** The progress stream put its recent events into `redactWorkStatusForShare`, which feeds both the browser-visible scheduler snapshot on `reach_list_nodes` and `dex doctor --share`. A `WorkEvent` carries `phase`, an operator-supplied label whose charset admits `/`, so `dex work run --phase feature/private-name` put a branch name into output whose entire purpose is to leave the machine, against an invariant whose evidence clause says in terms that share output omits repository paths, branches and PIDs. The existing DEX-INV-026 test passes `branch: 'feature/private-name'` and would have caught the branch field; the new progress test used `phase: 'event-proof'`, which is label-shaped, so nothing exercised a path-shaped phase. Proved by counterexample before the fix: a lease acquired with `phase: 'feature/private-name'` produced a share payload carrying `"phase":"feature/private-name"` twice. The share projection now drops `phase`, and `phase` leaves the `SchedulerEventSnapshot` wire type so it cannot be put back by accident; the local `workStatus` is unchanged and still carries the label. Two smaller repairs: the live proof harness had been loosened to take the trace id from `structuredContent`, `_meta` **and** any trailing text block it could parse as JSON, which makes the `live-mcp-surface` assertion unfalsifiable across exactly the shapes it exists to tell apart, and it returned `texts[0]` rather than joining every block, silently truncating a multi-block payload — it now reads `_meta` alone and treats every text block as payload, and the counterfactual confirms a gateway carrying the id only as an extra content block now fails where the loosened harness passed; and `scripts/install-macos.ts` parsed the node's allowed roots as `.map(root => path.resolve(root.trim())).filter(Boolean)`, resolving before filtering, so a trailing `:` in `DEX_REACH_ALLOWED_ROOTS` wrote the installer's working directory into the worker's 0600 config and put its roots hash out of step with the node's, leaving the worker permanently unusable — it now filters before resolving, matching `loadNodeConfig` exactly. A test also pins the delegation allowlist against the operation catalog: the worker runs `nativeCall` with a fixed `workspace-safe` profile rather than the effective profile authorization produced, which is equivalent **today** only because all three delegated operations are `readOnlyAllowed`; adding one that read-only refuses would let the worker execute, under owner READ-ONLY, what the node itself refuses. Authority boundary checked and intact: delegation sits at the end of `executeOperation`, after the profile and secret refusals and after authorization, and both call sites pass `reservation.decision.effectiveProfile`. **Recorded, not changed, because they are design calls in the owner's subsystem**: heartbeats and observation cache hits each now append to the same 200-entry history ring, so `lease-acquired`, `lease-reclaimed` and `ticket-expired` are evicted by routine noise within a couple hundred polls, and `recordWorkEvent` re-reads, re-validates and rewrites the whole file under a second lock held inside the coordinator lock on every heartbeat; and the trace-id metadata key is still written as three separate string literals after its rename from `com.stinkyweasel.dexreach/trace-id` to `dex-reach/trace-id`.
- **r52** — Repaired one rendering defect in r51 and reconciled `docs/EXPANSION_RECORD.md` with it. The DEX-INV-042 bullet added to section 4 was appended with a literal `\n` rather than a newline, so it rendered glued to the end of the DEX-INV-041 bullet with the escape visible; it is now its own list item, with the text unchanged. The shareable record was then brought in line with r51 and is materially different from what it said an hour ago: the release blocker is no longer post-install client acceptance but `PND-010`, installing the CI-green candidate on the primary Mac and taking one real routed call through the repaired response contract, because the Mac is still running a `6e01e6c`-era build. The owner-action list drops the Android item, the browser-client item, the second-machine item and the Linux item, and leads with that reinstall; the pending-items paragraph now names four open items (`PND-001`, `005`, `009`, `010`) and states plainly that `PND-003`, `PND-004` and `PND-007` were reclassified out of pending work rather than proven, and that a reader should look at the `UNV-` entries for the second-machine and Linux gaps; the lead, the Phase 3 note and the install bullet all now say that the installed build is behind the branch; and the commit ledger gains the trace-metadata round. **The owner's Mac-side evidence in r51 is recorded in the record as the owner's own observation and is explicitly not re-verified from this container**: the Samsung SM-X910 identity reads, the real Claude Code `reach_fingerprint` call against the installed runtime, and the install itself. Verified from here before writing: the four PROOF STALE marks in `docs/INVARIANTS.md` are untouched by r51 and still count 4, so DEX-INV-005, 009, 017 and 021 remain stale; the manifest carries 42 invariants; `UNV-002` and `UNV-003` are still present, which is what makes the PND-003/PND-004 reclassification coherent rather than a quiet deletion. Documentation only. No source, invariant manifest, policy, roots, credentials, service configuration, install state or public MCP action contract was touched.
- **r51** — Closed the stale owner-present evidence and isolated the final release blocker. Real Android hardware is verified through DEX on a Samsung SM-X910 over wireless ADB, including DEX-visible enumeration and harmless manufacturer/model/Android/SDK identity reads. Claude Code is authenticated in the owner HOME and completed a real installed-runtime `reach_fingerprint` call with only the DEX fingerprint tool pre-authorized. The branch now contains DEX-INV-042 for the coordinator daemon transport boundary. Current GitHub head `1ad4a65` is mergeable and green: CodeQL PASS and DEX validation PASS, with `validate`, `runtime-proof`, and `reproducible-build` all successful; loopback runtime proof is 19 proven / 0 failed / 3 unverified and includes the full six-stage causal trace plus caller trace metadata. Second-machine, Linux, and pristine-host proofs remain honest non-blocking unknowns. The only release blocker is final installation/real-client acceptance of the latest trace-result head on the primary Mac. That reinstall could not be completed in this session because Remote Desktop Commander is offline and the platform blocks DEX-routed mutation commands before they reach the node; PR #2 therefore remains draft/unmerged rather than being falsely declared done.
- **r50** — Repaired `runtime-proof`, red again on `096a248`/`3d98de5`. Those commits moved the caller-visible trace id off a second text content block and onto the tool result's `_meta` under `com.stinkyweasel.dexreach/trace-id`, which is the correct place for it: an extra content block is joined into the payload by any client that concatenates content, which is exactly what this repository's own proof harness did. The gateway and its regression test moved together; the proof client in `scripts/lib/live-reach.ts` did not, so it kept parsing content blocks, found no trace id, and `live-mcp-surface` failed with `the routed MCP result did not expose a caller-visible trace id`. Reproduced in this container before the change and green after. The harness now reads `_meta` and treats every text block as payload again, restoring the `join('\n')` the extra-block workaround had displaced. The failure was producer/consumer drift on a string constant that is written in three places; a single exported constant shared by the gateway and this harness would make the next rename impossible to get wrong, and is deliberately left to the owner because `src/gateway/mcp.ts` is under active edit. Post-fix gate in this container: `npm run proof -- --require-live` **19 proven, 0 failed, 3 unverified**; typecheck PASS; invariant manifest PASS at **42**; `npm test` **249 tests, 248 pass** with the single failure the known environmental `native.test.ts` `nvm` case; build PASS; production audit **0 vulnerabilities**; backend probe; `git diff --check` clean. Only `scripts/lib/live-reach.ts` and this file changed. No gateway, node, invariant, policy, roots, credential, service or public MCP action contract change, and no test or gate weakened, skipped or disabled.
- **r49** — Repaired the red `runtime-proof` job on `5b08354` and reconciled the record for the 42nd invariant. That commit added a caller-visible trace id to routed MCP results and asserted it in the `live-mcp-surface` proof, but it asserted it on `reach_list_tools`, which the gateway answers from the tool list the node advertised at registration. That request never reaches the node, so there are no authorize/execute/receipt stages and no trace to expose; CI reported **18 proven, 1 failed, 3 unverified** with `the live MCP result did not expose a caller-visible trace id`, reproduced identically in this container. The assertion was not deleted: a gateway-local action is now required to carry **no** trace id, because handing a caller an identifier that resolves to nothing the node did is manufactured evidence, and the caller-visible trace-id assertion was kept where it is meaningful, on the routed `reach_trust_report`. Counterfactual confirmed: making `reach_list_tools` emit a trace id turns the new assertion red, and restoring the file leaves no diff. The routed assertion passes on its own merits, so `5b08354`'s gateway/node trace plumbing is sound and only the assertion's target was wrong. Post-fix gate in this container: `npm run proof -- --require-live` **19 proven, 0 failed, 3 unverified**; typecheck PASS; invariant manifest PASS at **42**; `npm test` **249 tests, 248 pass** with the single failure the known environmental `native.test.ts` `nvm` case; build PASS; production audit **0 vulnerabilities**; `26-tool` backend probe; `git diff --check` clean. **PND-016 is closed** by `DEX-INV-042`, which `5b08354` added to `docs/INVARIANTS.md` and `src/shared/invariants.ts`; the owner-action list and the shared record were reconciled to match, along with the invariant and test counts in the validation table. PND-010 is **not** closed by this: a caller-visible trace id now appears on a routed loopback call, but PND-010 requires it from a real client against the deployed path and resolving through `npm run dex -- trace <id>` as the complete chain. Only `scripts/proof-run.ts` and the two documentation files changed; no gateway, node, invariant, policy, roots, credential, service or public MCP action contract change was made, and no test or gate was weakened or skipped.
- **r48** — Reconciled `docs/EXPANSION_RECORD.md` and its shared copy against the installed state recorded in r47. The record was materially wrong in the most consequential direction: it still instructed readers not to install the branch on the primary Mac, and the branch was already installed and running there. Corrections applied: the lead now states the installed runtime state; the DO-NOT-INSTALL line is replaced by the install plus the fact that it preceded a completed `npm run verify:golden`; the commit ledger counts against `6e01e6c`, the last commit to change runtime code (29 commits, 91 files, 15,711 insertions, 335 deletions), rather than against a tip that moves each time the record is corrected; `6e01e6c` and `1d82c74` are added as ledger rows; the DEX-INV-040 and DEX-INV-041 proof-state cells now read installed-runtime rather than source-CLI; the validation section is retitled to `6e01e6c` and distinguishes the container gate this session ran from the focused 42-test gate the owner ran on the Mac; the honest-limits section separates container evidence from installed-Mac evidence and states explicitly that `installed and running` is not `proven against a real client`; Phase 3 is now gated on PND-008 rather than on installation; and the owner-action list is rewritten to lead with post-install client acceptance, add PND-016 and PND-004, drop the workspace-safe item closed by PND-011, note that the PND-009 and PND-013 decisions are already made, and leave the project license as the one open owner-only question. Verified before writing: the four PROOF STALE marks in `docs/INVARIANTS.md` still stand (count 4, unchanged); PND-008 was redefined as post-install client acceptance rather than closed; `fresh-node-install` remains a proof-harness item gated on `DEX_REACH_PROOF_ALLOW_INSTALL=1` and is not satisfied by `npm run install:macos`. This revision changed documentation only. No source code, invariant manifest, policy, roots, credentials, service configuration, install state, or public MCP action contract was touched, and nothing here is fresh evidence about the primary Mac.
- **r47** — Reconciled the primary Mac and GitHub branch into one linear history and installed the resulting runtime. The three local state/documentation edits were first committed, then rebased onto GitHub commit `6e01e6c` rather than stashed or discarded; the sole conflict was the competing r45 entries in this file, resolved by preserving GitHub r45 unchanged and renumbering the installed-runtime reconciliation to r46. The reconciled head then passed **42/42** focused coordinator/machine-capacity/work-run/MCP-contract tests on the primary Mac, including the new foreign-socket ownership regression, plus typecheck, the **41-invariant** manifest, build, production audit **0 vulnerabilities**, the **26-tool** backend probe, and `git diff --check`. `npm run install:macos` rebuilt and staged coordinator/gateway/node, delegated replacement to the one-shot launchd helper, produced the expected brief disconnect, and the node re-registered. Live post-install evidence: coordinator PID 24204, gateway PID 24285, node PID 24291; all three launchd jobs running with one run and no exit; `/healthz` healthy with one online node; same `full-local` profile, roots, client ceilings, ON mode, and no grants. GitHub DEX validation and CodeQL are green on `6e01e6c`. The daemon transport hardening still lacks its own invariant ID, now tracked as PND-016. No owner policy, roots, credentials, client ceilings, grants, profile, or public MCP action contract were widened by this reconciliation.
- **r46** — Reconciled the new handoff against live repository/runtime evidence rather than trusting the summary. Before the GitHub socket-hardening commit was fetched, local HEAD and the then-stale local origin ref both resolved to `7a0e9c1ad314f0e01bf6e2d4f68433742dd5551e`; the working tree was clean before this state-only reconciliation. This entry records that observed installed-runtime state and does not claim that `7a0e9c1` remains the current source head after reconciliation. The primary Mac is already running the new persistent three-service runtime: coordinator PID 92898, gateway PID 92903, node PID 92908, all launchd `state = running`, `runs = 1`, `last exit code = (never exited)`, with healthy loopback `/healthz` and one online node. Installed `work-status --share` showed one substantive/heavy slot, queue latency fields, and no active/queued work at the idle observation; installed `activity --share` exposed privacy-safe activity. A live installed cross-path proof acquired a lease through the owner CLI and observed the same active lease from the isolated compatibility HOME, clearing the stale source-only claim for DEX-INV-041. The focused coordinator/daemon/MCP verification chain ran under managed `work-run` and exited 0: 41 focused tests, typecheck, 41-invariant manifest, production audit threshold, and 26-tool backend probe. The handoff's full `npm run verify` final exit remains unconfirmed because that earlier run's terminal exit was not captured; a new heavy full-verify attempt correctly queued under CPU load and was cancelled rather than duplicating work. No source code, policy, roots, credentials, service configuration, install state, or Git publication was changed by r46; this revision corrected stale documentation only.
- **r45** — Reviewed `7a0e9c1` (local coordinator daemon and queue status) and repaired one defect in it. The daemon is a Unix socket, not a network listener: it creates its directory 0700, refuses a path that is not a socket, refuses one another account owns, refuses to displace a live daemon, bounds its frame size, rejects trailing data after the frame, and checks a protocol version. When no socket exists the client falls back to the original direct file-lock path, which is the conservative mode 0A.18 asks for; when the daemon exists but does not answer it raises rather than bypassing admission. The new `SchedulerSnapshot` crossing to MCP clients carries counts and latencies only -- no paths, PIDs or command text -- and the new work-bundle CPU, memory, I/O and network budgets only ever add blocking reasons, so they narrow and cannot widen.
  - **Defect found: a client trusted any socket at the daemon path.** The daemon verifies account ownership before taking the path over, but `socketPresent` in `src/coordinator/client.ts` checked only that the path was a socket. The path is a digest of a non-secret state directory under `os.tmpdir()`, so on a host whose temporary directory is shared between accounts -- Linux `/tmp`, unlike macOS's per-user `/var/folders` -- another local account could create it first and answer every admission call. Separately, `listen` created the socket under the process umask and only then chmod'ed it to 0600, leaving a window in which it was connectable. The client now refuses a socket owned by another uid rather than trusting it or silently falling back to direct mode beside a scheduler it cannot see, and the daemon binds under a 0177 umask so the socket is never exposed. Counterfactual confirmed: removing the ownership check turns the new regression red.
  - **Bounding the impact honestly.** This was not an authority breach. Admission grants no execution authority (DEX-INV-022), so a forged decision could not widen what any request may do; it could make DEX oversubscribe the machine or refuse all work. It was not reachable on the primary Mac, whose temporary directory is per-account.
  - **The daemon has no invariant yet.** Its account-private transport and its refusal to coordinate through a foreign socket are release-blocking behaviour by this project's own standard, and an invariant for them is the owner's to mint rather than this session's, since the subsystem is his.
  - Verified at this head in an ephemeral Linux container: typecheck, the 41-invariant manifest, **248 tests with 247 passing** (the one failure is the environmental `native.test.ts` case from r15), build, production audit 0 vulnerabilities, `git diff --check`, and `npm run proof` at **19 proven, 0 failed, 3 unverified**. `docs/EXPANSION_RECORD.md` reconciled.
- **r44** — Independent verification of `3fb0eaf` and the `dc7b341` merge in an ephemeral Linux container, not on the primary Mac. Full gate at that head: typecheck, the **41-invariant** manifest, **245 tests with 244 passing** (the one failure is the environmental `native.test.ts` case from r15), build, production audit **0 vulnerabilities**, the **26-tool** compatibility probe, `git diff --check`, and `npm run proof` at **19 proven, 0 failed, 3 unverified** with the strengthened `workspace-safe-profile` item passing through a real local pair. All CI jobs are green on `dc7b341`.
  - **Reviewed for widening, and none found.** `safeProcessLabel` keeps only the first token of a command, basenames it and accepts it only against `^[A-Za-z0-9._+:-]{1,48}$`, falling back to the literal `process`, so no argument, path fragment or credential can reach the persisted ledger; a malformed record throws rather than emptying the store, which is the behaviour the r26 sweep established for corrupt state. `machineStateDir()` re-anchors only coordination and activity to the OS account home, leaving authority-bearing `stateDir()` semantics untouched, and an explicit `DEX_REACH_STATE_DIR` still isolates both. DEX-INV-040 and DEX-INV-041 describe what the code does.
  - **Repaired an ordering defect introduced by the merge.** The `OPERATIONAL_STATE.md` conflict resolution renumbered this session's entry to r43 but left it between r36 and r35, breaking the newest-first ordering this log uses. It has been moved to the top. Its content is unchanged and still describes `7f617ab`.
  - `docs/EXPANSION_RECORD.md` reconciled to `dc7b341`: 26 commits, 41 invariants, 245 tests, and the coordinator hardening described in the shared-machine section. Documentation only; no source, test, workflow or dependency file touched.
- **r43** — `7f617ab`, committed by the owner rather than by this program: Electron and Chromium helper processes are no longer counted as anonymous heavy coding workloads in machine admission. Such helpers inherit their host application's command line (the observed case was ChatGPT.app's `Codex (Renderer)` at 185% CPU), so a desktop application's UI process tree read as an independent coding session and consumed a substantive slot. `looksHeavy` now returns false for the documented Chromium helper types (`renderer`, `gpu-process`, `utility`, `zygote`), matched as an exact `--type=` token so an unrelated CLI flag still classifies normally.
  - **This loosens one signal, and that is worth stating plainly.** A genuinely CPU-saturating helper no longer subtracts an admission slot. It does not loosen the ceiling itself: `evaluateCapacity` classifies CPU pressure from load average, and memory and thermal pressure from their own probes, independently of `observedUncoordinatedHeavy`, so real contention from such a process still queues heavy work. DEX-INV-024 is unchanged and stays regression-only.
  - Verified here at `7f617ab`, not on the primary Mac: typecheck, 39-invariant manifest, 233 tests with 232 passing (the one failure is the environmental `native.test.ts` case from r15), build, and a proof run of 19 proven, 0 failed, 3 unverified. All three CI jobs are green on this commit. `docs/EXPANSION_RECORD.md` was reconciled to it.
- **r42** — Reproduced and bounded the intermittent connector `UNAVAILABLE` incident without crossing DEX's private-state guard. During the strengthened heavy proof, a connector call again failed before returning while the proof itself continued and completed successfully. Safe audit records show successful ChatGPT node requests immediately before and after the gap and no failed node request for the dropped call; launchd still reports gateway PID 947 and node PID 951 with `runs = 1`, `last exit code = (never exited)`, and continuous runtime; `/usr/bin/log show` over the reproduction window contained no event for either service PID. This excludes a gateway/node process restart and demonstrates that the dropped call failed upstream of node execution, but it does not identify the exact connector/platform/network component. The launchd stdout/stderr paths are inside the protected DEX private-state directory and were not bypassed. PND-015 is therefore closed as an executed diagnostic: the remaining uncertainty is an upstream-platform root cause, not an unperformed DEX reproduction. Separately, the current ChatGPT tool registry was inspected directly and still exposes exactly **12** DEX connector actions, confirming PND-001 remains open; the server's 16-action contract is not the issue. No install, deploy, commit, push, owner-policy change, profile change, root change, or credential change occurred.
- **r41** — Closed the remaining owner-independent runtime proof gaps that were waiting on Mac capacity. Once host memory/CPU pressure returned healthy, a correctly classified heavy mutate lease ran the strengthened `npm run proof -- --require-live` against the current tree. Result: **19 proven, 0 failed, 3 unverified** in 71.83s. The upgraded `workspace-safe-profile` item passed through a real OAuth/MCP local pair with typed write + checkpoint admitted, shell/process/session surfaces refused, and READ-ONLY narrowing preserved; `live-mcp-surface` reported exactly 16 first-class actions, 22 compatibility tools with 4 withheld, and a trust report scoped to 6 live-evaluated invariants of 41. `fresh-node-install` remains the only required proof unproven; Android and second-physical-machine items remain hardware-unavailable. PND-012 then passed through a separate real temporary source node on the primary Mac: an `other`-client rolling budget of two operations admitted two live fingerprint calls, refused the third specifically for budget exhaustion, and left the owner-policy hash unchanged before configuration and after usage. The installed 0.3.2 runtime, public HTTPS gateway, owner policy, roots, profile, credentials, commit, and push state were not changed.
- **r40** — Completed the current-tree source/build gate without bypassing machine admission. A serial full-regression runner held its own read lease and passed **241/241 tests** at test concurrency 1, including the new virtual-HOME machine-state regressions and the long nonce-capacity/isolation stress cases; typecheck, the **41-invariant** manifest, and `git diff --check` passed in the same runner. A separate coordinated build gate then passed a fresh TypeScript build, production `npm audit --omit=dev --audit-level=high` with **0 vulnerabilities**, the raw **26-tool** compatibility probe, and another diff check. This supersedes r39's 'complete suite pending' statement for source/build verification. The strengthened full `npm run proof` remains pending because it is classified as heavy work and the Mac still reports memory pressure warning. No install, deploy, commit, push, owner-policy change, profile change, root change, or credential change occurred.
- **r39** — Continued owner-absent source hardening without crossing the install gate. A real two-path Mac contention test exposed a correctness defect: Desktop Commander intentionally virtualizes `HOME`, and coordinator/activity paths followed that virtual home, so compatibility-launched `npm run dex` commands saw a second scheduler and activity ledger on the same physical Mac. Two regressions were added first and failed pre-fix; `machineStateDir()` now anchors only machine coordination/activity to the real OS account home while ordinary `stateDir()` keeps authority-bearing state on the virtual/explicit state root. A third regression locks that separation. Targeted local-env/activity/coordinator tests pass 27/27, typecheck passes, DEX-INV-041 raises the release manifest to 41 invariants, and live cross-path smoke proves a compatibility shell with isolated HOME sees the native lease/services. PND-006 then passed for real: a compatibility-shell `claude-code` contender queued behind the native ChatGPT mutate lease and native status/queue/activity saw the same ticket. PND-011 also passed for source on the primary Mac: a temporary real gateway/node/OAuth/MCP pair ran `workspace-safe`, admitted typed write + checkpoint, refused shell and process/session compatibility surfaces, and READ-ONLY narrowed it correctly; probe exit 0 and teardown were observed. The permanent proof matrix now requires `workspace-safe-profile` from a `local-pair` rather than helper-only in-process checks. A full `npm run proof` rerun was requested under a heavy coordinator ticket but remained queued for five minutes under sustained memory warning, so the ticket was cancelled rather than bypassing the machine-pressure gate. PND-009 direction is now a dedicated authorization server after the current migration gate; PND-013 is no public capability-request action in this release. The installed 0.3.2 runtime, public HTTPS gateway, roots, owner policy, profile, credentials, commit, and push state were not changed.
- **r38** — Narrowed OBS-001 without bypassing DEX's private-state boundary. launchd reports the installed gateway/node still on PIDs 947/951 with one run each and no exit; current loopback health reports 0.3.2 with one online node; safe audit output shows normal successful requests. A direct attempt to tail private DEX logs through remote execution was refused by the private-state guard and was deliberately not bypassed. Therefore the intermittent connector `UNAVAILABLE` responses observed during high-load proof work are not evidenced service crashes/restarts and happened before node execution, but the responsible upstream layer remains unknown. No code, install, deployment, commit or push resulted from this forensic pass.
- **r37** — Closed the primary-Mac source/runtime proof gap as far as the current non-installing environment permits. The complete repository suite ran under a DEX heavy lease and passed **238/238** in 187.5s; the two nonce-capacity tests consumed roughly 84s and 79s and completed normally rather than hanging. Typecheck, the synchronized **40-invariant** manifest, a fresh build, production audit (**0 vulnerabilities**), the raw **26-tool** backend probe, and `git diff --check` all pass. The owner activity ledger was exercised against real queue/lease pressure during this work and correctly separated DEX work from gateway/node services. `npm run proof` then ran on the primary Mac without installation and produced **19 proven, 0 failed, 3 unverified**: `fresh-node-install` remained unverified because installation was not authorized, while Android and second-physical-machine proof remained unavailable hardware. The first run exposed an SDK warning that the proof client's OAuth provider did not persist SEP-2352 discovery state; `scripts/lib/live-reach.ts` now stores and restores `OAuthDiscoveryState` with the same in-process durability as its PKCE verifier, typechecks, and a second proof run returned the same 19/0/3 result without that warning. Separately, real Claude Code 2.1.236 completed protected-resource/auth-server discovery, dynamic registration, DEX owner approval, token exchange, MCP initialize and `tools/list` against an isolated migrated source gateway on the Mac. A DEX tool action from Claude remains unverified because Claude's own account OAuth is expired on this Mac; even a no-tool Claude prompt failed with the same account-auth error. Temporary proof gateway/node processes and temporary Claude MCP entries were removed. No branch install, deployment, commit or push occurred; the persistent 0.3.2 gateway/node remain the installed runtime and PND-008 still gates installation.
- **r36** — Added owner-visible DEX activity/process evidence without widening authority. `src/shared/activity.ts` keeps a bounded node-local lifecycle ledger for actual native child PIDs and compatibility `start_process` PIDs while persisting no raw command arguments, prompts, output or credentials; corrupt evidence fails visibly. `dex activity` separates DEX-owned processes, coordinated work, queued work, persistent gateway/node services and anonymous heavy blockers, with watch/json/history/share modes, and the Dock console exposes the view locally. Coordinator leases now distinguish an explicitly bound workload PID from the short-lived CLI process that acquired an unbound lease. Added DEX-INV-040. Validation on the primary Mac source tree: typecheck PASS, 40-invariant manifest PASS, 5/5 activity tests PASS, combined activity/coordinator subset 23/23 PASS, build PASS, production audit 0 vulnerabilities, raw compatibility probe 26 tools, and live source `dex activity` correctly showed gateway PID, node PID and the current ChatGPT lease as workload-PID-unbound. Share mode omitted local PID/path detail; final review also found and repaired the single-record `activity <id> --share` path so it applies the same redaction. A real queued heavy-regression ticket was rendered with its executor, repository, workload, access and phase while gateway/node remained separate. The complete test suite was not started because the coordinator continued to report memory warning plus saturated CPU and kept the heavy ticket queued. No install, deployment, commit or push; the persistent 0.3.2 services remain unchanged and PND-008 still gates installation.
- **r35** — Added `docs/EXPANSION_RECORD.md`, a single owner-facing record of the whole expansion: the 21-commit ledger, what each phase added, all nine defects found and fixed with what was observed in each case, the 39 invariants and their proof state, the 22 proof items with the environment that can establish each, what was deliberately not done, and the numbered list of what only the owner can do. It restates evidence already recorded in this file, the invariant manifest and the commit messages; it establishes nothing new and changes no behaviour. Documentation only: typecheck and the 39-invariant manifest pass, and no source, test, workflow or dependency file is touched.
- **r34** — Extended the proof run to the served MCP contract, and stated precisely what that does and does not close. A real OAuth/PKCE client of the current SDK, against a running v2 gateway on a loopback pair, listed exactly the 16 first-class actions, received exactly the 22-tool compatibility surface for the node with the 4 withheld tools **absent rather than refused**, and fetched a live trust report carrying a certificate hash and a scope naming 6 live-evaluated invariants of 39 with the explicit statement that it does not replace release proof. That is the part of DEX-INV-005, 017 and 021 which had only ever been source-proven since the MCP v2 migration.
  - **The PROOF STALE marks are not cleared.** All four still stand, and each row now says what the loopback run established and what it did not. The gateway ran over loopback HTTP, so DEX-INV-009's non-loopback requirement is untouched; the client was the SDK, not ChatGPT or Claude; and nothing here is the deployed HTTPS ingress. PND-008 is unchanged: `npm run verify:golden` and a real client session on the primary Mac are still what clears them.
  - **Phase 3 stays deferred and DO NOT INSTALL stays in force.** A loopback pair on Linux is runtime proof of the software path, not of the owner's machine, and the gate was written about the deployed gateway rather than about any running gateway.
  - Proof run at this commit: **19 proven, 0 failed, 3 unverified** — `fresh-node-install` needs a macOS host its owner authorizes, and the Android and second-machine items need that hardware.
- **r33** — Phase 15: physical and runtime proof. The question this phase answers is the one every previous phase deferred: does any of this actually happen on a machine, or is it only true of the source?
  - **A real pair, not a simulation.** `scripts/lib/live-reach.ts` starts a real gateway and a real node agent as separate OS processes on a loopback port inside a temporary state directory, enrolls the node through the owner CLI, and drives it with a real MCP SDK client through the full OAuth flow. `scripts/proof-run.ts` (`npm run proof`) runs every proof the current environment can establish and records the rest as unverified. It never opens the owner's real state directory and never contacts a deployed gateway.
  - **It found two defects on its first executed run, neither of which any source test could have seen.**
    - The gateway's OAuth discovery document advertises `authorization_response_iss_parameter_supported` — the SDK's auth router emits that claim by default — but DEX//REACH emits its authorization response from its own `/dex/approve` route rather than from inside `/authorize`, so the SDK's `withIssOnCallbackRedirect` wrapper never saw it and `iss` was never appended. Per RFC 9207 a client that reads that claim MUST refuse a response without it, so **every spec-compliant MCP client stopped at the callback with an issuer mismatch**, against a gateway that answered `/healthz`, served tools and looked healthy. `npm run smoke` would have failed the same way. Fixed in `ReachOAuthProvider.approve`, with the issuer handed in explicitly so it is the same string the discovery document publishes; `smoke.ts` and `sim-two-nodes.ts` now pass the whole authorization response to `finishAuth` instead of a bare code, so they exercise the check rather than bypassing it. New invariant DEX-INV-039, and a regression test whose counterfactual was confirmed: removing the line turns it red with `expected 'https://example.invalid/', actual undefined`.
    - After an owner revokes a node, forgets it and re-enrolls it, the node host still holds the transport private key from before, while the gateway forgot the matching public key with the rest of the record. The node presented that proof, was refused at the websocket upgrade with no reason given (deliberately, so a prober learns nothing), and **retried the same refused proof forever** — no log line on either side said why. The node now alternates credentials when a connection is refused before it opens, and says in its own log which credential it is about to try and that the owner must re-enroll if both fail. This cannot widen anything: the gateway still decides, and the existing regression that a migrated asymmetric node is refused a bearer token still holds.
  - **What is actually proven, and where.** 18 of the 19 required items were established in a single run on a Linux container: a fresh node resolving to OFF and a corrupt policy file failing closed; the execution fingerprint of the real host; READ-ONLY admitting inspection; workspace-safe refusing shell and refusing a widening compatibility tool; a typed write landing inside its root while writes outside it and through a symlink are refused and leave no file; shell refused under both narrow profiles; a grant exhausting at one use and refusing after expiry; a rolling budget refusing the third of two; **20 separate OS processes against a 5-slot concurrency ceiling admitting exactly 5**; 7 processes against the coordinator's measured 1 substantive slot admitting exactly 1; a six-stage linked trace that refused the content offered to every span; three signed chained receipts where an edit and a reorder each break verification; an evidence bundle verifying offline with three claims that stay unproven by construction; and over the live pair — explicit node targeting with an unknown id refused and no fallback, the complete Ed25519 enrollment ceremony with private-key material refused and the bearer token afterwards rejected, a write refused end to end while READ-ONLY with the file never created, the kill switch refusing the next call on an already-authorized MCP session, revocation dropping the live socket and keeping a retrying agent out, and forget-plus-re-enroll bringing the same node id back with no surviving transport key.
  - **What is not proven.** `fresh-node-install` requires a macOS host whose owner authorizes an install (`DEX_REACH_PROOF_ALLOW_INSTALL=1`) and stays `UNVERIFIED — HARDWARE NOT AVAILABLE`, as do the Android and second-machine items. **None of this is proof about the owner's Mac, launchd, Keychain or the deployed public gateway.** A loopback pair on Linux proves the software path; it does not prove the installation.
  - **The honesty rule is enforced in code, not by discipline.** `reconcileProofRun` in `src/shared/proof-matrix.ts` is the only place a result may come into existence, and it does not trust its caller: a `pass` reported for an environment that was not available is discarded and said to have been discarded, a `fail` reported for absent hardware becomes unverified because missing hardware is not evidence of breakage, an unattempted item says it was not attempted rather than being omitted, and an observation naming an unknown or duplicated item is an error rather than a line that vanishes. New invariant DEX-INV-038, with the anti-fabrication cases tested directly.
  - **Validation in an ephemeral Linux container:** typecheck, 39-invariant manifest, 232 tests with 231 passing (the one failure is the environmental `native.test.ts` case from r15), build, production audit 0 vulnerabilities, and two executed proof runs.
- **r32** — Phase 14: release provenance. The question this phase answers is whether the artifacts in `dist/` are a function of the tracked source at one commit and nothing else, which a successful build does not establish.
  - **The comparison.** `scripts/lib/clean-build.ts` exports the commit with `git archive`, installs from `package-lock.json` alone, and builds in a temporary directory with a deliberately different absolute path and no pre-existing output, then compares every artifact by hash. That catches an untracked build input, a stale artifact, a global package the workflow never installs, and a path or timestamp baked into the output. `npm run verify:clean-build` runs it, and a new `reproducible-build` CI job runs it on every push and pull request, separately from `validate` so a failure is attributable.
  - **It was run, not just written, and it found something on its first real run.** `dist/src/node/backend.js` was still present in the working tree's build output although `src/node/backend.ts` was deleted in Phase 11 — exactly the stale-artifact case, and one that matters because `install:macos` installs from `dist/`. `npm run build` now clears `dist/` first through a `prebuild` step. The comparison was then executed against a clean checkout of `4067898`: **89 artifacts rebuilt byte for byte**, in a different directory, with a fresh dependency install.
  - **The manifest states what it did not do.** `npm run release:provenance` writes `release/manifest.json`, `release/checksums.txt` and a CycloneDX `release/sbom.cdx.json`, recording the commit, working-tree cleanliness, toolchain, every check with its result, a SHA-256 per artifact, and a hash of the SBOM normalized so npm's per-run serial number and timestamp do not make two builds of one tree look different. A check that could not run is recorded with its reason rather than omitted. `live deployment` is always UNVERIFIED. macOS, Android and second-machine claims read `UNVERIFIED — HARDWARE NOT AVAILABLE` unless the checks genuinely ran there.
  - **Sweep findings in the new code, four fixed.** The platform claim required every check to read PASS, and the manifest always carries a check that never can (`live deployment`), so a completely successful run would have reported the platform it ran on as unverified — "we tested nothing" on a run that tested plenty. It now requires only that nothing failed. A build that fails in a clean checkout escaped as an exception and came out under the "could not be tested" exit code, reading as an environment problem when it is the strongest form of the failure this check exists to find; it is now reported as a reproducibility failure. `checksumTree` treated a symlink as neither file nor directory and skipped it silently, so a tree with one and a tree without compared equal. And the compatibility-adapter probe was downgraded to SKIPPED on any failure, which would have hidden a probe that ran and genuinely failed; it is now UNVERIFIED with the real message attached.
  - **Also corrected:** the `validate` workflow ran typecheck, tests, build and audit but never `npm run invariants -- --check`, although `npm run verify` does. The suite covered it indirectly through the manifest-synchronization test; the explicit step is now there too.
  - **Not done, deliberately.** No licence was chosen; `package.json` still reads `UNLICENSED`. Nothing is signed or attested, and no release was published.
  - Added DEX-INV-037 and `tests/provenance.test.ts` (9 tests).
  - Validation in an ephemeral Linux container: typecheck, 37-invariant manifest, 220 tests with 219 passing (the one failure is the environmental `native.test.ts` case from r15), build, production audit 0 vulnerabilities, and the clean-build comparison executed end to end against a real checkout.
- **r31** — Phase 13: portable evidence bundles. A completed action can now be handed to someone who does not have the node, does not have DEX and does not trust the person handing it over.
  - **The format.** `src/shared/evidence.ts` builds a JSON bundle from identifiers, hashes, signed receipts, sanitized trace spans and its own list of limitations. Receipts go in verbatim because they are signed and any edit destroys the only thing that makes them evidence; spans are re-sanitized on the way in rather than trusted, so a span written by an older build cannot ride a field that is no longer allowed into a shareable artifact. Owner CLI: `evidence export [--trace ID] [--receipt ID]... [--out FILE] [--disclose FILE]` and `evidence verify <file>`.
  - **No overall verdict, deliberately.** Verification answers fourteen claims separately and each one explains what its answer does not mean. Four can never be PASS from inside a bundle and are always reported unproven: node identity, bundle completeness, trace completeness and external side effect. The most important of those is the first — signatures verify against the key the bundle itself carries, so a forger can produce an internally flawless bundle, and binding one to a real node means comparing a key fingerprint against a key the reader already trusts. A test builds exactly that forgery: the whole chain re-signed under an attacker key, every structural claim passing, zero failures, and the fingerprint different. That is the test that justifies the whole shape of the output.
  - **Content exclusion proved by search, not by assertion.** The export test drives real file content, a real command line, real stdout and real owner policy through receipts, then searches the serialized bundle for each of them. A future field that quietly carried content would pass a field-absence check and fail this one.
  - **Disclosures are opt-in.** A request hash cannot be recomputed without the arguments, so on its own it proves nothing to a third party. `--disclose` attaches arguments the owner chose to publish, and the verifier recomputes the hash and compares; a disclosure that understates what was actually run fails rather than being accepted. The claim reads NOT INCLUDED when no disclosure is attached, never PASS.
  - **Sweep findings in the new code, three fixed.** A bundle could be relabelled as any node: the node id sat outside the signatures, and recomputing the bundle checksum — which anyone can do, since it is a checksum and not a signature — hid the edit completely. Observed pre-fix: a bundle whose receipts all name `real-node` verified with zero failures while the verifier's own summary printed `andrews-trusted-mac`. The verifier now cross-checks the label against the signed receipts. Second, a bundle whose `receipts` or `spans` array held `null` threw out of the verifier instead of failing, so a stranger's malformed bundle produced a stack trace that reads like a tooling problem rather than a bad bundle; every element is now shape-checked before any claim runs. Third, an export that was asked for receipts it could not find reported `no receipts matched` without naming them, and a draft of the message suggested raising `--limit` when the receipt reader is capped at 100 regardless; the refusal now names the missing ids and states the real window, which is also recorded as a limitation inside every bundle.
  - Added DEX-INV-036 and `tests/evidence.test.ts` (12 tests).
  - Validation in an ephemeral Linux container: typecheck, 36-invariant manifest, 211 tests with 210 passing (the one failure is the environmental `native.test.ts` case from r15), build, production audit 0 vulnerabilities, and `evidence export` / `evidence verify` exercised end to end against an isolated state directory with a leak check on the written file. No bundle has come from a deployed node, so this is regression-only and the branch remains DO NOT INSTALL.
- **r30** — Phase 12: EXPERIMENTAL node-local secret broker. A remote AI can now ask for a command that needs a credential without ever being told the credential.
  - **The shape.** `src/shared/secrets.ts` holds values in `nodes/<node>.secrets.json` at mode 0600. The model names an alias; `resolveSecrets` is the only path from an alias to a value, it is called from exactly one place in the tree, and that place is after final authorization and immediately before the child process starts. The value is injected as an environment variable for that one process and never onto a command line, where the process table would publish it to every other process on the machine. Owner CLI: `secrets`, `secret set <alias> --env NAME`, `secret rm <alias>`; `--value`, `--secret` and `--password` are refused outright rather than warned about, terminal input is not echoed, and a piped value works for scripting.
  - **Using a secret is its own authority.** `secret.use` was added as a seventh capability, and `requiredCapabilities` derives it from the request rather than the operation catalog, because the same `dex.process.run` is a plain shell call in one request and a credential-bearing one in the next. Holding every other capability combined does not grant it, and holding it alone grants no shell to inject into.
  - **Proving the negative rather than asserting it.** The central test does not check that redaction was remembered. It drives a real value through a real child process, a plan, the audit log, a signed receipt, a trace and both doctor reports, then walks every byte written under the state directory looking for the value itself and names any artifact that holds it. It was shown to fail: with a one-line debug append reintroduced in `resolveSecrets`, the test failed naming `resolve-debug.log`. A second, structural test asserts the import graph — only `src/node/native.ts` may call `resolveSecrets`, the gateway may not import the broker at all, and any new importer fails the test until it is reviewed deliberately.
  - **Sweep findings in the new code, both authority defects, both fixed.** First, READ-ONLY admits `dex.process.run` through a branch of `authorizeOperation` that returns before `grantRequired` is ever consulted, so in READ-ONLY a client holding no capability grant at all could name a stored alias. Observed pre-fix: under the `read-only` profile, `ps ewwaux` — which is on the read-only allowlist and prints the calling process's own environment — ran with the injected variable present, and only the best-effort output scrubber stood between that and disclosure. READ-ONLY and `workspace-safe` now refuse injection at the dispatcher, at the executor and at the injection primitive itself; the grant check was not relocated, because moving it would change what READ-ONLY means for every other operation. Second, `dc.call` nests its arguments and already demanded `secret.use`, but nothing on that path resolves an alias, so an owner would have authorized a credential-bearing call and the adapter would have received a credential-free one, silently. An operation that cannot inject a named alias now refuses the request.
  - **A third gap closed while writing the tests.** The scrubber skips values shorter than six characters so it does not turn ordinary text into `[REDACTED]`. A shorter value would therefore have been stored and injected but never scrubbed, with nothing saying so, so `setSecret` now refuses it and says why.
  - **What the broker does not claim.** It does not make arbitrary shell safe. Scrubbing catches a value echoed verbatim, which is the common accident; it cannot catch one the command encoded, split or forwarded, and a test asserts that limit rather than leaving it as prose. `doctor` now reports the alias count, and alias names only when the report is not shareable, since a name like `acme-prod-deploy` describes infrastructure.
  - **The remote route was made deliberate.** No first-class MCP action accepts a secret alias; the only remote path is `reach_plan`'s free-form target arguments, which makes a credential-bearing call exact, one-use, identity-bound and recorded before it runs. That was true by accident and is now asserted in `tests/mcp-contract.test.ts` against the served schemas, so the direct route cannot be opened without deliberately widening the published surface. The action count stays 16.
  - Added DEX-INV-035 and `tests/secrets.test.ts` (11 tests).
  - **r28's open item is closed.** The production dependency audit now passes: `npm audit --omit=dev --audit-level=high` returns 0 vulnerabilities, both in the working tree and against a clean copy of `package.json`/`package-lock.json` with no `node_modules` present, under the same npm 10.9.7 that returned the misleading `400 Invalid package tree` earlier. The lockfile never changed, which is what r28 predicted: the failure was the registry's maintenance window, not the tree.
  - Validation in an ephemeral Linux container while holding a coordinator lease: typecheck, 35-invariant manifest, 199 tests with 198 passing (the one failure is the environmental `native.test.ts` case from r15, which fails identically at the base commit and passes on CI), build, production audit 0 vulnerabilities, and the owner CLI exercised end to end against an isolated state directory. No node has brokered a secret, so this is regression-only, Phase 3 remains deferred, and the branch remains DO NOT INSTALL.
- **r29** — Phase 11: capability adapter contract. Third-party tooling DEX routes to now declares itself and is checked, rather than being trusted or hand-filtered at the call site.
  - **The contract.** `src/shared/adapter-contract.ts` defines a manifest in which every tool declares its required DEX capability, risk class, mutation, network reach, path-bearing argument names, workspace-safe eligibility, plan eligibility, remote blocking, reversibility and checkpoint strategy. `decodeAdapterManifest` refuses an absent or malformed field rather than defaulting it, because the safe-looking default is the one an incomplete manifest would benefit from. `admitAdapterManifest` then checks every declaration against DEX's own catalog and refuses any disagreement, so a manifest can only fail to admit a tool and never widen one. A single refusal aborts the whole install.
  - **Desktop Commander refactored into it.** `src/node/backend.ts` became `src/node/adapters/desktop-commander.ts`, with its declaration in `desktop-commander.manifest.ts` as adapter-supplied data that imports nothing and is typed `unknown`, so it must pass the same fail-closed decoder a third-party manifest would. It is deliberately not generated from DEX's catalog; if it were, checking it against that catalog would prove nothing. Adapter version and entry-point hash are read from disk at load and overwrite whatever the manifest claims. The node now filters the running adapter's tool list through the registry rather than through an ad-hoc block set, so an adapter that starts offering a new tool cannot widen the surface, and it refuses to start if the adapter fails to offer a tool DEX admitted.
  - **Path arguments were probed, not guessed.** The declared `pathArguments` for all 26 tools were read from the installed adapter's own MCP schemas rather than inferred from names. That is where `write_pdf`'s second path argument `outputPath` and `edit_block`'s `file_path` come from.
  - **Capability laundering through the wrapper, found and closed.** `activeGrant` matched on `operationCapability(operation)`, which for `dc.call` is `compat`. A grant holding `compat` alone therefore authorized `write_file` and `start_process` through the adapter without ever holding `file.write` or `process.shell` — the wrapper laundered capability exactly as an unresolved risk class launders risk. `requiredCapabilities` now returns both the wrapper's capability and the named tool's, and a call naming no tool or an unclassified one demands all six, which no grant holds. No existing test covered the old behavior, so nothing depended on it. This narrows authority and grants none.
  - **Dead code removed with it.** The `set_config_value` branch that claimed to permit remote safety-configuration mutation under `full-local` was unreachable: the tool is remote-blocked and was already filtered out of the surface before that branch could run. It is gone rather than left implying a way in.
  - **Sweep findings in the new code, both fixed.** The manifest completeness check compared every manifest against the whole catalog, which would have refused any second adapter for failing to declare Desktop Commander's tools; tools now carry an `adapter` attribution and the check is scoped to the declaring adapter, with an adapter claiming another adapter's tool refused outright. Installing two adapters that provide one tool name would have made routing depend on install order; that is now refused, and with per-tool attribution it is defence in depth rather than a reachable path, which is recorded as such rather than tested.
  - Added DEX-INV-034 and `tests/adapters.test.ts` (18 tests): undeclared tool, unknown risk, missing capability, understated capability, hidden and invented path arguments, network declaration mismatch, workspace-safe over-claim, plan wrapper capability inheritance, withheld tools staying withheld and indistinguishable from absent, identity evidence, the 22-tool surface not widening, a manifest that omits a tool, structural rubbish, a mutating tool claiming no undo, enforcement reading the catalog rather than a mutated manifest object, per-adapter scoping, and cross-adapter tool claims.
  - Validation in an ephemeral Linux container while holding a coordinator lease: typecheck, 34-invariant manifest, 188 tests with 187 passing (the one failure is the environmental `native.test.ts` case from r15), build, 26-tool backend probe. The production dependency audit could not be observed because npm's registry maintenance window was open; see r28. No node has started under the adapter registry, so this is regression-only, and the branch remains DO NOT INSTALL.
- **r28** — CI audit gate repaired after the `validate` workflow went red on `7729f9f` at the `npm audit --omit=dev --audit-level=high` step. Everything else on that run passed, including all 170 tests, so nothing in the diff was implicated: `package.json` and `package-lock.json` are byte-identical to `c4bc3e0`, where the same workflow passed eleven hours earlier.
  - **Root cause is an npm registry maintenance window, not the lockfile.** Node 22 bundles npm 10, whose `npm audit` falls back to the registry's retired quick-audit endpoint, and that endpoint answers every failure with `400 Invalid package tree, run npm install to rebuild your package-lock.json`. That message is misleading. npm 11.19.1 and 12.0.2 against the identical tree instead report `503 Service Unavailable` from `/-/npm/v1/security/advisories/bulk` with the body `We are currently performing maintenance`, and https://status.npmjs.org lists scheduled maintenance for 2026-09-19 17:00-19:00 UTC. Both CI runs, 17:08 and the 17:13 re-run, fall inside that window.
  - Ruled out before concluding that, each with no `node_modules` present so the installed tree is not a factor: the default branch `main`'s own lockfile returns the identical 400; this branch's returns the identical 400; and `npm install --package-lock-only` does not change the result. The lockfile is `lockfileVersion: 3` with no linked, aliased or `file:` dependencies and no workspaces.
  - **Repair.** `.github/workflows/ci.yml` now pins `npm@11.19.1` before `npm ci`, so the audit uses the supported bulk advisory endpoint and reports the real registry status instead of a fabricated lockfile complaint. Pinned exactly rather than tracking `latest` for reproducibility; npm 11's engine range (`^20.17.0 || >=22.9.0`) covers the workflow's Node 22. The audit step gained a bounded retry that rides out a registry transport failure.
  - **The gate is not weakened.** The command is still `npm audit --omit=dev --audit-level=high` and the step still fails the job on any high or critical production advisory. Nothing was removed, no `|| true`, no lowered threshold, no `continue-on-error`, no suppressed exit status. The retry loop's semantics were tested directly: a command that always fails still exits 1 after the final unguarded run, a command that succeeds passes with no retries, and a command that fails twice then succeeds passes on the third attempt.
  - **What could not be proven.** The audit cannot be observed green while the maintenance window is open, so this repair is proven only to the extent of the endpoint change and the retry semantics. Re-verify after 19:00 UTC on 2026-09-19.
- **r27** — Second independent adversarial pass over Phases 6 through 10, run without trusting r26's own report. Two further evidence-supported defects were found, both in code r26 had already looked at, and each was reproduced against `c4bc3e0` before being fixed.
  - **The nonce capacity introduced in r26 let one node deny every other node authentication.** Failing closed at capacity was right, but the ceiling counted every node's nonces together, and the capacity check runs before signature verification, so nothing frees a slot until a nonce's window closes. Observed pre-fix: a correctly enrolled, correctly signing node issued 4095 ordinary proofs and a second, idle node's next proof was refused `nonce-capacity` — roughly 13.7 proofs per second sustained, which is ordinary traffic rather than an attack. Capacity is now counted per node, so a node can exhaust only its own share; `MAX_NONCES` remains as a hard bound on the persisted map, set high enough that no single node can reach it alone. r26's replay regression still passes unchanged. DEX-INV-033 restated to include the per-node scoping.
  - **A failed capability-request approval left a live grant the request log did not explain.** `approveCapabilityRequest` created the grant first and recorded the decision second, so any failure of the second step left authority in owner policy while the request still read `pending` or `expired` with `grantId: null`, and the owner had been told the approval failed. Observed pre-fix: with the request expiring during the grant write, the owner saw `capability request has expired` while one live `file.write` grant existed in owner policy, recorded nowhere. The decision is now recorded before the grant is created, so a failure below it leaves the decision durable and no authority granted; the error says so explicitly. r26 examined this same function and concluded only that `createGrant` is idempotent on grant id, which is true and is a different property — that is why this pass did not take r26's review note as coverage. DEX-INV-030 restated to include the ordering.
  - **Concurrency proof, run rather than reasoned about.** The mandatory 20-against-capacity-5 check exists in `tests/budget.test.ts` and passes, but it is in-process. It was also run across 20 separate operating-system processes reserving against one node's budget: exactly 5 admitted, 15 refused with the owner's concurrency message, 5 inflight slots on disk, and no lock timeouts, which is what actually exercises the cross-process file lock.
  - Re-derived from source rather than from r26's conclusions and found sound: CLI budget ceilings reject non-integers, negatives, `Infinity` and `NaN`, and `parseDuration` is bounded to 1s..7d; clearing a budget resets usage only when no rule remains, so it cannot refund consumed authority; `restorePolicyRevision` re-runs `policyCheck` and every custom assertion before persisting, so a restore cannot re-admit assertion-forbidden authority; the websocket upgrade path fails closed on an unknown authorization scheme, a proof failure or any thrown error; `/node/enroll` refuses private-key material and returns an undifferentiated error; `dex doctor --share` drops repo root, upstream, hostname, platform and per-client ceilings, and the error strings it keeps carry grant ids and owner-authored notes but no filesystem paths; transport keys and receipt keys remain distinct files with distinct generation sites and no caller loads one for the other's use.
  - Validation in an ephemeral Linux container, not on the primary Mac, while holding a coordinator lease: typecheck, 33-invariant manifest, 170 tests with 169 passing (the one failure is the environmental `native.test.ts` case from r15), build. No install, no deployment, no smoke. Phase 3 remains deferred, the branch remains DO NOT INSTALL, and the PROOF STALE marks on DEX-INV-005, 009, 017 and 021 still stand.
- **r26** — Adversarial correction pass over Phases 6 through 10. Five evidence-supported defects were found by source and data-flow inspection and each was reproduced against `22286d6` before being fixed.
  - **Budget laundering through the compatibility adapter (fail-open, the significant one for budgets).** `requestedAuthorityCost` resolved a `dc.call`'s mutation and risk from the named tool correctly, but read the write payload and process timeout from the *outer* arguments, so a compatibility write charged 0 bytes and a compatibility process request charged 0 ms. Observed pre-fix: the same 4096-byte write cost 4096 natively and 0 through the adapter, and a 30s timeout cost 30000 natively and 0 through the adapter, so `maxRequestedWriteBytes` and `maxRequestedProcessMs` could both be exceeded by routing the identical action through `dc.call`. Cost extraction now recurses into nested arguments and accepts either naming convention, mirroring `extractPaths` in `security.ts`, which walks nested arguments for the same reason.
  - **Per-client budgets measured every client.** `evaluateRule` filtered samples by window only, so a per-client rule's rolling usage included other clients' traffic; `maxConcurrent` in the same function already scoped itself by client, which is what showed the inconsistency was accidental. Observed pre-fix: with a `claude` ceiling of 2 operations, claude's *first* request was refused after four unrelated chatgpt requests. Rolling dimensions now agree with concurrency. This one over-restricted rather than over-permitted, so it created no authority.
  - **Abandoned concurrency slots were never released.** `pruneUsage` passed `inflight` through untouched and nothing expired it, so a crashed holder kept its slot forever and the array grew without bound across crashes; a `maxConcurrent` budget would refuse every request until the owner reset usage by hand. Observed pre-fix: a slot abandoned 24 hours earlier still occupied the ceiling. A slot now records its pid and is reclaimed only when it is both well past due AND its process is gone, which is the rule the work coordinator already uses (DEX-INV-025); reclaiming never signals the process and a live holder is never evicted for being slow. Samples still inside a window are never dropped, because discarding one would refund consumed authority.
  - **A corrupt capability request log was silently emptied and then overwritten.** A failed decode returned an empty list, so the next `createCapabilityRequest` wrote that empty list back plus the new entry. Observed pre-fix: after corrupting the file, the existing request became invisible and its id was gone from disk once a new request was filed — destroying the record that explains why a still-live grant exists. Corrupt state now throws and the file is left untouched for the owner, which is how `AccessState` and budget usage already behave.
  - **Nonce eviction opened a replay window (the security-relevant defect).** `pruneNonces` evicted the oldest entries by expiry once the cache passed 4096, including nonces still inside their five-minute validity window, and the replay check is purely presence in that map. Observed pre-fix: a captured proof, correctly refused as `replay` while remembered, **authenticated successfully** after the cache filled. The cache now drops only nonces whose window has closed, and a proof arriving when every slot holds a still-replayable nonce is refused as `nonce-capacity` — failing closed instead of forgetting something replayable. Added DEX-INV-033 and a node-transport-authentication validation subset.
  - Reviewed and deliberately left alone: `dex doctor` performs no writes; policy assertions can only append errors and so can only reject owner policy; `createGrant` is already idempotent on grant id, so a retried or raced approval cannot mint a second grant; the access-lock-then-budget-lock order is universal and `budget-usage.ts` never imports `access.ts`, so no reverse path exists; authorization still precedes budget consumption, so a refused preauthorization consumes nothing. `riskFloorForCapability('inspect')` returning `inspect` understates `dex.plan`, which carries the `inspect` capability at `typed-mutate` risk, but that is asserted by an existing test and plan/commit bypass grant matching entirely, so it is recorded here rather than changed.
  - Validation in an ephemeral Linux container, not on the primary Mac, while holding a coordinator lease: typecheck, 33-invariant manifest, 167 tests with 166 passing (the one failure is the environmental `native.test.ts` case from r15), build, 0 production vulnerabilities, 26-tool backend probe. No install, no deployment, no smoke. Phase 3 remains deferred, the branch remains DO NOT INSTALL, and the PROOF STALE marks on DEX-INV-005, 009, 017 and 021 still stand.
- **r25** — Added asymmetric node transport authentication as a separate Ed25519 domain from receipt signing. Gateway node-auth v2 stores public keys, one-use enrollment tokens and a bounded nonce cache; private keys never persist there. Legacy bearer nodes remain valid; explicit enrollment of a transport public key enters `migrating`; `complete-migration` makes the node asymmetric-only and refuses silent downgrade. Websocket upgrade accepts `DexNodeEd25519` proofs or bearer tokens. Transport keys live in `nodes/<id>.transport.ed25519.pem` (0600), not the receipts key path. File-backed storage is the proven store; Keychain is not claimed. Added DEX-INV-032. Validation on the primary Mac: typecheck, 32-invariant check, 160/160 tests, production build, 0 production vulnerabilities, 26-tool backend probe. No live gateway/node pair has completed asymmetric enrollment, so this is regression-only. Do-not-install and PROOF STALE marks from r17 still stand.
- **r24** — Fixed capability-request risk classification. Unknown operations are refused instead of being recorded as inspect. Capability-only requests take the highest catalog-derived risk of the requested capabilities and never default to inspect. DEX-INV-030 wording updated to match. Request creation still grants no authority. Validation on the primary Mac: typecheck, 31-invariant check, 154/154 tests, build, 0 production vulnerabilities, 26-tool backend probe. Do-not-install and PROOF STALE marks from r17 still stand.
- **r23** — Added read-only `dex doctor` with `--json`, `--deep` and `--share`. The report is evidence-scoped: source/branch/dirty, source version (explicitly not installed-service proof), owner policy, budgets, requests, assertions, coordinator slots/pressure/leases/queue, and the 16-action MCP contract with PROOF STALE caveats. `--share` redacts repository paths, upstream and hostname. `--deep` adds launchd load state without treating load as health. Validation on the primary Mac: typecheck, 150/150 tests, production build, 0 production vulnerabilities, live `doctor --share`.
- **r22** — Added custom policy assertions and append-only policy history. Assertions are regression tests against the real policy engine, not a second authorizer: candidate owner state is checked with built-in policyCheck plus custom assertions before persist. History records owner authority mutations and not grant-use counters. Restoring an old revision creates a new revision. Owner CLI: `assertions`, `assertion add/clear`, `policy-history`, `policy-restore`. Added DEX-INV-031. Validation on the primary Mac: typecheck, 31-invariant manifest, 149/149 tests, production build, 0 production vulnerabilities.
- **r21** — Added a local capability-request lifecycle. AI may ask; only the owner grants. `src/shared/capability-requests.ts` stores pending/approved/denied/expired requests outside AccessState. Creating a request does not create a grant and does not change the owner policy hash. `request approve` creates an ordinary CapabilityGrant, may narrow, and cannot widen. Deny and expiry create no grant. OFF still refuses execution after approval. Justifications are bounded and credential-shaped text is refused. Raw secret-bearing arguments are hashed, not stored. Node operation `dex.capability.request` is classified but not advertised on the public MCP surface. Added DEX-INV-030. Validation on the primary Mac: typecheck, 30-invariant manifest, 146/146 tests, production build, 0 production vulnerabilities. Do-not-install and PROOF STALE marks from r17 still stand.
- **r20** — Added rolling execution budgets that only narrow authority. `src/shared/budget-policy.ts` holds owner-configured shared and per-client ceilings; `src/shared/budget-usage.ts` holds rolling samples and inflight concurrency slots outside AccessState so usage cannot change the policy hash. `reserveOperation` takes the access lock then the budget lock, authorizes, reserves cost, then consumes a grant; a denial at authorize consumes nothing. `releaseBudgetConcurrency` releases only the inflight slot. Indirect `dex.commitPlan` inherits target cost and is budgeted at the inner target reservation so a wrapper cannot launder shell past an inspect-sized budget. Owner CLI: `budgets`, `budget set <shared|client> --window … --max-*`, `budget clear <id>`. Added DEX-INV-029. Validation on the primary Mac while holding a coordinator mutate lease: typecheck, 29-invariant manifest, 140/140 tests, production build, 0 production vulnerabilities, 26-tool backend probe, live `dex budgets` against the installed node (no budget configured). Do-not-install and PROOF STALE marks from r17 still stand. No deployed node has enforced a live budget.
- **r19** — Added the `workspace-safe` execution profile without adding an owner mode. `src/shared/profiles.ts` holds the profile list, the standing refusals and the profile descriptions; `src/shared/operations.ts` gained `effectiveWorkspaceSafe`, a per-tool resolution marker and the workspace-safe tool set; `src/node/config.ts` now validates the profile from that one list instead of restating it. Enforcement is repeated at the executor, the planner, the command grammar and the compatibility tool guard, and planning is refused at plan time so a workspace-safe node never issues a plan it would not honour or takes a checkpoint for one. The composition rule is the substance of the phase: the constraint is read from the node's configured profile, not from the effective profile authorization produced, because READ-ONLY replaces that value and would otherwise re-admit the shell the profile exists to refuse. Ten tests prove the three owner modes are unchanged, that OFF, READ-ONLY, client ceilings and grants each still override or narrow the profile, that an undeclared adapter tool and an unclassified operation fail closed, that a planned commit inherits its target rather than laundering risk, and that the profile is a strict subset of a wider one. `dex explain` now reports both narrowings and whether the operation would actually run, rather than a policy answer that could contradict the node. Added DEX-INV-028. Validation in the same ephemeral Linux container while holding a coordinator lease: typecheck, 28-invariant manifest, 123 tests with 122 passing (the same environmental `native.test.ts` case as r15), build, 0 production vulnerabilities, 26-tool backend probe. No installed node's profile changed, the default is still `development`, and no node has yet run with the new profile configured.
- **r18** — Added end-to-end causal tracing so a completed action can be reconstructed from evidence rather than from narration. `src/shared/trace.ts` implements W3C Trace Context creation and continuation, a span schema that is an explicit allowlist, a bounded local trace store and an OpenTelemetry export that is off by default; the node emits `node`, `authorize`, and `plan`/`commit`/`execute` spans and returns the `traceId` on both the success and the refusal path; `trace` and `traces` were added to the owner CLI. Redaction is structural, not advisory: a span cannot carry arguments, content, stdout/stderr, raw plan arguments or credentials, a refusal is traced as an outcome class with its message omitted because that message can quote a path or a command, and `baggage` is refused outright since a control plane has no reason to propagate arbitrary caller-controlled state. Malformed inbound trace context starts a fresh trace instead of being repaired or trusted. Added DEX-INV-027 and `tests/trace.test.ts`. Validation in the same ephemeral Linux container, while holding a coordinator lease: typecheck, 27-invariant manifest, 113 tests with 112 passing (the one failure is the same environmental `native.test.ts` case described in r15), build, 0 production vulnerabilities, and a 26-tool backend probe. The trace store and both CLI commands were dogfooded directly; the synthetic trace used for that was deleted afterwards so the local store holds no fabricated evidence. No deployed gateway has produced a live trace, so tracing is regression-only and the PROOF STALE marks from r17 still stand.
- **r17** — Migrated the MCP dependency from `@modelcontextprotocol/sdk` 1.30 to the official v2 package split at 2.0.0 and zod 3 to 4, without changing served protocol semantics. Package existence and API shape were verified against the npm registry and the real type declarations before any dependency change rather than assumed: the v2 packages, the `NodeStreamableHTTPServerTransport` drop-in for the sessionful route, and the `server-legacy/auth` bridge for the gateway's own authorization server were all confirmed to exist and to typecheck. Added `tests/mcp-contract.test.ts`, which connects a real MCP client to the server over an in-memory transport and asserts the exact 16 action names, deterministic ordering across two independent server instances, descriptions, annotations, required `node_id` on every node-targeted action, and the `reach_plan` target enum — proving the served surface rather than the source text. Local validation: typecheck, 26-invariant manifest, 105 tests with 104 passing (same environmental `native.test.ts` case), build, 0 production vulnerabilities. The deployed smoke has NOT rerun, so DEX-INV-005, 009, 017 and 021 are marked PROOF STALE and this branch must not be installed on the primary Mac until `verify:golden` and a real client session pass there.
- **r16** — Centralized DEX operation authority metadata in `src/shared/operations.ts` and routed every previously duplicated classification through it: capability mapping, READ-ONLY admission (split into inspect operations served directly and delegated shell/compatibility operations constrained further down), plan target eligibility, checkpoint strategy, and the remote compatibility block list. Added a risk classification for all 26 compatibility adapter tools, deterministic `AuthorityCost` for the later budget phase, and fail-closed lookups for unclassified operations and tools. Writing the tests exposed that `dex.commitPlan` declared shell risk while carrying the `inspect` capability, so risk inheritance from the planned target is now an explicit descriptor field rather than a special-cased operation name. No authorization outcome changed: capability mapping and OFF/READ-ONLY/ON admission are asserted equal to the pre-catalog implementation across every catalog, gateway-only and unknown operation. Validation in the same Linux container: typecheck, 26-invariant manifest, 100 tests with 99 passing (the one failure is the same environmental `native.test.ts` case described in r15), build, 0 production vulnerabilities. The 16-action public MCP contract and the 26/22/4 compatibility split are unchanged and now regression-locked.
- **r15** — Added the shared-machine work coordinator as the first phase of the DEX expansion program. Introduced deterministic host capacity measurement (memory-primary and CPU-secondary slot ceilings, live memory/CPU/thermal pressure, uncoordinated-workload observation from the process table only), per-repository exclusive mutation leases, a FIFO work queue with anti-jump ordering, heartbeat-and-dead-process staleness reclaim that never signals another process, bounded local history, and seven owner CLI commands. Added invariants DEX-INV-022 through 026 and 29 focused tests covering all fifteen required coordinator proofs. Dogfooding the coordinator on its own implementation surfaced and fixed two real defects: the caller's own process tree was counted as competing anonymous load, and the `dex-reach` control CLI was misclassified as a persistent DEX service. All validation ran in an ephemeral Linux container, not on the primary Mac: typecheck, 26-invariant manifest, 82 tests (81 pass; one pre-existing unrelated `native.test.ts` failure reproduced identically at base commit `80fcbe1` and caused by that container's shell profile printing `nvm`), production build, and 0 production vulnerabilities. No install, no deployment, no smoke, and no change to the 16-action public MCP contract or any authorization path. macOS pressure probes are fixture-tested only and remain unverified on macOS.
- **r14** — Added bounded local project portfolio controls to the owner CLI without widening remote authority. New `projects`, `dirty`, and `project <query> info|checkpoint` commands discover repositories under the home root (or an explicitly allowed root), report branch/dirty/upstream state, refuse ambiguous project matches, and reuse the existing DEX checkpoint mechanism for reversible project snapshots. Discovery and per-repository Git inspection are concurrency-bounded for large portfolios. Real-Mac built-CLI smoke enumerated 103 repositories under `/Users/andrew`; focused portfolio tests passed 2/2; complete `npm run verify` passed with synchronized 21-invariant manifest, 53/53 tests, production build, 0 production vulnerabilities, and 26-tool raw compatibility probe. The public 16-action MCP contract and node authority model remain unchanged.
- **r13** — Added DEX//REACH 0.3.2 trust proof as an executable control rather than a display-only fingerprint. Introduced the synchronized 21-ID machine invariant manifest and `npm run invariants`; added evidence-scoped `reach_trust_report`; bound every new exact-action plan to a fresh execution fingerprint and optional caller-supplied expected identity; made commit refuse machine/user/cwd/repository/branch/remote/runtime drift before mutation; added regression tests plus live public smoke for wrong-identity refusal and real Git branch drift with no write. Deployed through the self-hosted macOS installer: the expected brief transport interruption occurred, gateway/node returned on new PIDs, node re-registered as 0.3.2, owner ON/full-local policy, roots, client ceilings, and no-grant state were preserved. Final `npm run verify:golden` passed with 21 invariant IDs, 51/51 tests, build, 0 production vulnerabilities, 26 raw compatibility tools, exactly 22 safe remote compatibility tools, 16 public MCP actions, trust report PASS, identity-bound plan/commit, signed receipts, and checkpoint proof. DEX correctly refused remote access to its own private install-state path during verification. This already-open ChatGPT connector still exposes the older 12-action cache, so direct UI/tool discovery of plan/commit/receipts/trust remains unverified even though public MCP runtime proof is green.
- **r12** — Exhaustive 0.3.1 hardening/golden-worker sweep. Closed symlink/plural/camelCase scope bypasses, policy stale-write rollback, plan double-claim, receipt-chain races, node-auth/revocation/bootstrap/OAuth persistence races, OAuth client-ID injection, public HTTP/remote WS transport gaps, compatibility safety-config/history/vendor/URL-proxy exposure, child-process credential-environment leakage, launchd Homebrew PATH/ADB false-positive behavior, self-hosted installer self-termination/respawning-helper failures, and Dock launcher Terminal/TCC mismatch. Reproduced and repaired a second receipt fork caused by stale-lock recovery deleting a newer owner's lock. Final local/deployed proof: 48/48 tests, three complete post-lock regression passes, five focused lock/receipt stress passes, build, 0-vulnerability production audit, 26-tool raw backend probe, 15-tool public MCP, exact 22-tool remote compatibility surface, ADB available, plan→commit, signed receipts, checkpoint, and signed Dock app opening a new Terminal without changing node PID or authority. Publication/hosted-check state is intentionally read from Git/GitHub rather than self-asserted inside the commit being checked.
- **r11** — Historical Dock persistence repair: exact Dock URL verification replaced substring detection; launcher/icon/signature/pin were verified. Superseded by r12's shell-app Terminal control launcher.
- **r10** — Historical Dock detection correction: restricted detection to `persistent-apps` and reverified the pinned app path.
- **r9** — Introduced the primary-Mac Dock launcher and documented the then-observed forced-restart teardown problem. Superseded in r12 by bounded backend close and the self-host-safe one-shot installer lifecycle.
- **r8** — Added 0.3.0 READ-ONLY fail-closed hardening, capability grants, policy assertions, exact plan/commit, signed receipts, 15-tool MCP metadata, CI/CodeQL workflows, and adversarial tests; deployment was still on the older public surface at that time.
- **r7** — Sanitized public operational evidence and separated deployment-specific/private identifiers from public repository documentation.
- **r6** — Reconciled public-source onboarding and added private vulnerability-reporting guidance without weakening enrollment/runtime authority.
- **r5** — Added node-local access policy, second-device enrollment/install flow, explicit routing/no fallback, actor-aware audit, local control CLI, fail-closed policy, and two-node simulation/real-client policy checks.
- **r4** — Repaired ChatGPT OAuth/app action discovery, added MCP metadata/classification, proxy handling, checkpoint smoke fixture, and client handshake evidence.
- **r3** — Added per-node credential migration/rotation, native file/process/repo/checkpoint/ADB paths, and Claude registration/authentication.
- **r2** — Implemented and validated the initial persistent gateway/node/OAuth/MCP path plus public HTTPS ingress and recovery proof.
- **r1** — Initialized the authoritative operational record from repository/runtime evidence.
